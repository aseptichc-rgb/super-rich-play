import {TYPES,assetValue,assetSaleReport,developmentQuote,canBuild,assetLandValue,buildingArea,buildingAnnualGrowth,footprintCells,parcelQuote,coords,OFF_ROAD,BOULEVARD} from './engine.js';
import {propertyPartyPanel} from './reputation.js';
const money=n=>'₲'+Math.round(n).toLocaleString('en-US');
export function assetSummary(s,i,report){
 const t=s.tiles[i],value=assetValue(s,i),land=t.tenure==='buy'?assetLandValue(s,i):t.deposit||0,rental=['rental','condo','office'].includes(t.type);
 const row=(label,value,cls='')=>`<div class="data-row"><span>${label}</span><b class="${cls}">${value}</b></div>`;
 return `<section class="asset-profit" aria-label="Current price and monthly profit">${row('Road access',report.loc.boulevard?'Boulevard · Land value +50% · Rent +30%':report.loc.access?'Roadside':'Off-road · Revenue at '+Math.round(OFF_ROAD.revenue*100)+'%')}${row('Lot area',buildingArea(t)+(buildingArea(t)===1?' tile':' tiles'))}${row('Building value growth / yr',(buildingAnnualGrowth(t)*100).toFixed(0)+'%')}${row('Current price · Sale value',money(value))}${row(t.tenure==='buy'?'Land appraisal':'Refundable deposit',money(land))}${row('Building & facility value',money(Math.max(0,value-land)))}${row(rental?'Expected monthly rent':'Expected monthly revenue',money(report.revenue))}${row('Monthly operating costs','−'+money(report.cost))}${['rental','condo'].includes(t.type)?row('Expected occupancy',report.occupancy+'%'):''}${row('Expected monthly profit',money(report.profit),report.profit>=0?'positive':'negative')}</section><p class="help">Current price is what you would receive if you sold now. Monthly profit reflects the current location and operating settings, excluding personal living costs and loan interest.</p>${t.tenure==='buy'&&t.type!=='atelier'?`<details><summary>Merge lots and build new</summary><p>Combine with neighboring lots up to 9 tiles. Your facilities inside the footprint are demolished and replaced by the new building.</p><div class="button-row">${['hotel','office','resort','rental','condo','cafe'].map(type=>`<button data-redevelop="${type}">${TYPES[type].name}</button>`).join('')}</div></details>`:''}`;
}
export function constructionContract(s,i,type,footprint={width:1,height:1}){
 const d=TYPES[type],q=developmentQuote(s,i,type,1,footprint),reason=canBuild(s,i,type,'buy',footprint),cells=footprintCells(i,footprint);
 const acquisitions=cells.map(j=>({j,t:s.tiles[j],q:parcelQuote(s,j)})).filter(({t,q})=>t.type&&t.owner==='npc'&&q);
 const removed=cells.filter(j=>s.tiles[j].owner==='player'&&s.tiles[j].type!=='plot');
 const options=value=>[1,2,3].map(n=>`<option value="${n}" ${n===value?'selected':''}>${n} ${n===1?'tile':'tiles'}</option>`).join('');
 return `<span class="eyebrow">${q.label} · Foot traffic ${q.loc.footfall}/100</span><h2>${d.icon||''} ${d.name} Merged Build</h2><p>${q.loc.boulevard?`Boulevard · Land value +${Math.round((BOULEVARD.land-1)*100)}% · Housing, apartment & office rent +${Math.round((BOULEVARD.rent-1)*100)}%`:q.loc.access?'Roadside · Base build cost and revenue':`Off-road · On completion a road is auto-connected for free across empty land. Without a path it operates off-road. Until connected, versus roadside: land value ${Math.round(OFF_ROAD.land*100)}%, build cost ${Math.round(OFF_ROAD.construction*100)}%, expected revenue ${Math.round(OFF_ROAD.revenue*100)}%. Upkeep is unchanged and revenue recovers once the road connects.`}</p><div class="field-grid"><label>Lot width<select data-footprint="width">${options(footprint.width)}</select></label><label>Lot height<select data-footprint="height">${options(footprint.height)}</select></label></div><p>Uses ${footprint.width}×${footprint.height} tiles extending right and down from the selected tile, ${footprint.width*footprint.height} tiles in total.</p><div style="display:grid;grid-template-columns:repeat(${footprint.width},1fr);gap:6px">${cells.map(j=>{const t=s.tiles[j],p=coords(j);return `<div style="padding:8px;border:1px solid #bacbb5;border-radius:6px;font-size:12px">${p.x+1}, ${p.y+1}<br>${t.terrain==='water'?'River':t.type?TYPES[t.type].name:'Empty land'}<br>${t.owner==='player'?'My asset':t.type==='extension'?'Part of a merged building':t.type?'Existing facility':'Land to buy'}</div>`;}).join('')}</div>${acquisitions.length?`<h3>Existing buildings to buy first</h3><div class="new-options">${acquisitions.map(({j,t,q})=>`<button data-acquire-parcel="${j}" ${s.mode!=='sandbox'&&s.money<q.total?'disabled':''}>Buy ${TYPES[t.type].name} · ${money(q.total)} incl. land</button>`).join('')}</div><p>Land cost and total below update after you buy the building.</p>`:''}<div class="asset-profit"><p>Build cost <b>${money(q.construction)}</b></p><p>Unowned land cost <b>${money(q.land)}</b></p><h3>Build contract ${money(q.total)}</h3><p>Expected monthly revenue ${money(q.revenue)} − Operating costs ${money(q.cost)}</p><strong class="${q.profit>=0?'positive':'negative'}">Expected monthly profit ${money(q.profit)}</strong></div><p class="help">Versus a 1-tile lot in the same location: build cost ×${Math.pow(q.area,1.05).toFixed(2)} · revenue ×${Math.pow(q.area,1.3).toFixed(2)} · building value +${(buildingAnnualGrowth({footprint})*100).toFixed(0)}%/yr.<br>Cash on hand ${money(s.money)}. Initial building value is 70% of build cost × area^0.15.</p>${removed.length?`<p><b>Rebuilding demolishes ${removed.length} of your existing facilities.</b> Prior purchase and expansion investment is added to the new asset's ledger; there is no demolition compensation.</p>`:''}<button class="primary full" data-contract="buy" ${reason?'disabled':''}>${reason||`Build on ${q.area} tiles${removed.length?' after demolition':''}`}</button>${q.area===1&&d.group==='business'&&!s.tiles[i].type?`<button class="full" data-contract="lease" ${canBuild(s,i,type,'lease')?'disabled':''}>Start on a 1-tile lease · ${money(q.construction+Math.round(q.land*.2))}</button>`:''}<p class="help">${s.mode==='sandbox'?'Sandbox: no purchase/build cost deducted.':'Purchase and construction are separate contracts. Buildings you buy stay your assets even if you cancel construction.'} Monthly profit varies with the current location.</p>`;
}
export function constructionDialog(s,i,report){const sale=assetSaleReport(s,i),gainLabel=sale.gain<0?'Expected sale loss':sale.gain>0?'Expected sale gain':'Expected sale result',gainSign=sale.gain<0?'−':'+',roi=sale.roi===null?'N/A':`${sale.roi>=0?'+':''}${sale.roi.toFixed(1)}%`;const t=s.tiles[i],d=TYPES[t.type],q=developmentQuote(s,i,t.type,t.level),cost=Math.round((t.constructionCost??d.cost)*.8*t.level);return `<span class="eyebrow">MY BUILDING · Lv.${t.level}</span><h2>${d.icon} My ${d.name}</h2>${t.type==='office'?'<p><b>Principal-protected sale</b> · Sells for at least your total investment (land, build, expansions).</p>':''}<p>${q.label} · Foot traffic ${q.loc.footfall}/100${q.waterfront?' · Riverfront':''}. Profit is deposited automatically each month.</p>${assetSummary(s,i,report)}${propertyPartyPanel(s,i)}<section aria-label="If you sell now" style="margin:16px 0;padding:16px;border:1px solid ${sale.gain<0?'#dfb9a8':'#b8cdb7'};border-radius:10px;background:${sale.gain<0?'#fff3ed':'#eef4e8'}"><div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;font-size:14px"><b>If you sell now${sale.estimated?' · Est.':''}</b><span>You receive <b>${money(sale.value)}</b></span></div><div style="display:flex;justify-content:space-between;gap:12px;align-items:baseline;flex-wrap:wrap;margin-top:12px;color:${sale.gain<0?'#a23f24':'#315f4f'}"><span style="font-size:14px">${gainLabel}</span><strong style="font-size:24px;overflow-wrap:anywhere">${gainSign}${money(Math.abs(sale.gain))} <span style="font-size:18px">(${roi})</span></strong></div><p style="font-size:13px;line-height:1.6;margin:10px 0 0">Versus initial investment ${money(sale.initial)} · Expansion costs excluded<br>Total invested incl. expansions ${money(sale.invested)}<br>Sale gain only; monthly operating profit excluded.${sale.estimated?'<br>No purchase record in the older save, so investment and gain are estimates.':''}</p></section><div class="meter"><i style="width:${t.level/3*100}%"></i></div><p>${t.level<3?`Expand for +${money(report.profit/t.level)} monthly profit!`:'Fully expanded! Build something new on the next lot.'}</p><div class="button-row"><button class="primary" data-action="upgrade" ${t.level>=3||s.mode!=='sandbox'&&s.money<cost?'disabled':''}>${t.level>=3?'Lv.3 Complete':'Expand · '+money(cost)}</button><button data-action="sell">Sell Building</button></div><p class="help">Cash ${money(s.money)} · Sale value ${money(assetValue(s,i))}<br>Use the profit to build again or reinvest in ‘Money Makes Money’.</p>`;}
