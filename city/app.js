import {pwaSettings,shortcutDialog,shortcutAdded} from './pwa.js';
import {SHORTCUT_HINT_KEY} from './shortcut.js';
import {motion} from './motion.js';
import {startupsDialog,startupQuote,startupEstimate,foundStartup,sellStartup,setStartupStrategy,refreshStartupEvent} from './startups.js';
import {L,lang,LANGS,switchLanguage,localized} from './i18n.js';
import {developerSession,sessionSave,setTestMoney} from './developer.js';
import {economyBanner} from './economy.js';
import {createLandmarkUI} from './landmark-ui.js';
import {landmarkPreview} from './landmarks.js';
import {propertyArt} from './property-art.js';
const landmarkStyles=document.createElement('link');landmarkStyles.rel='stylesheet';landmarkStyles.href='./city/landmarks.css';document.head.append(landmarkStyles);
import {donate,mediaInterview,hostPropertyParty} from './reputation.js';
import {reputationDialog} from './empire.js';
import {growthCard,growthDialog,growthModel} from './growth.js';
import {constructionDialog,constructionContract,assetSummary,trafficText} from './construction.js';
import {compoundCard,compoundDialog,compoundOption,buyCompound,withdrawCompound,ensureCompound} from './compound.js';
const compoundStyles=document.createElement('link');compoundStyles.rel='stylesheet';compoundStyles.href='./city/compound.css';document.head.append(compoundStyles);
import {chapterOf,chapterCard,chapterDialog,legacyDialog,endingDialog,foundFoundation,SCENARIOS} from './legacy.js';
import {freshGame,newGameDialog} from './new-game.js';
import {VERSION,FEEDBACK_URL,releaseFeatures,feedbackDialog,feedbackText,issueURL} from './release.js';
import {cloudClient} from './cloud-save.js';
import {createCloudUI} from './cloud-ui.js';
import {SUPABASE_URL,SUPABASE_KEY} from './supabase-config.js';
import {FIREBASE_CONFIG} from './firebase-config.js';
import {createAuth} from './auth.js';
import {accountHTML} from './auth-ui.js';
import {accountStore} from './account-save.js';
const cloudStyles=document.createElement('link');cloudStyles.rel='stylesheet';cloudStyles.href='./city/cloud.css';document.head.append(cloudStyles);
// app.js imports the engine's location() helper, so the browser address must be read from window.location.
const cloud=cloudClient({url:SUPABASE_URL,key:SUPABASE_KEY});
const devSession=developerSession(window.location.hostname,window.location.search,cloud.enabled);
const SAVE_KEY=devSession.saveKey;
const FEATURES=releaseFeatures(window.location.hostname,devSession.cloudSave);
document.documentElement.lang=lang;document.title=L('SUPER RICH — Grow your assets, enjoy your wealth');
import {rivalCard,ensureRival} from './rival.js';
const legacyStyles=document.createElement('link');legacyStyles.rel='stylesheet';legacyStyles.href='./city/legacy.css';document.head.append(legacyStyles);
const releaseStyles=document.createElement('link');releaseStyles.rel='stylesheet';releaseStyles.href='./city/release.css';document.head.append(releaseStyles);
// Month-end dialogs (ending, chapter, crisis, venture, event) are shown one after another.
let dialogQueue=[];
function nextQueued(){const d=dialogQueue.shift();if(!d)return;if(d.fn)d.fn();else openDialog(d.type,d.html);}
function crisisDialog(r){return L`<div class="crisis-card"><span class="eyebrow">LIQUIDITY CRISIS</span><h2>${r.restructure?L('Restructuring'):L('Fire sale')}</h2><p>${r.restructure?L('Cash has run dry three months running. Reputation −50. If cash flow does not recover, your assets keep selling at a discount.'):L('Month-end cash went negative, so assets were sold below market value.')}</p><div class="legacy-score">${r.fireSale.map(i=>L`<div><span>${esc(i.name)}</span><b>+${money(i.value)}</b><small>Fire sale price</small></div>`).join('')}</div><p class="help">Stocks sell at a 3% discount; compound accounts, buildings and artwork at 30%. Review upkeep and income on the Cash Flow tab.</p><button data-action="close" class="primary full">Check Cash Flow</button></div>`;}
import {flexDialog,buyFlex,hostParty,flexState} from './flex.js';
import {artDialog,artViewDialog,buyArtwork,artPortfolio} from './art.js';
import {photoViewer} from './collection-photos.js';
import {vehicleDialog,chooseVehicle} from './luxury-models.js';
const vehicleStyles=document.createElement('link');vehicleStyles.rel='stylesheet';vehicleStyles.href='./city/luxury-models.css';document.head.append(vehicleStyles);
const photoStyles=document.createElement('link');photoStyles.rel='stylesheet';photoStyles.href='./city/collection-photos.css';document.head.append(photoStyles);
import {mansionDesign,mansionDialog,mansionPreview,roomCapacity,buildMansion,DEFAULT_MANSION,MANSION_HIT,mansionOwnerDialog,sellMansion} from './mansion.js';
import {mansionArtURL} from './mansion-art.js';
let mansionDraft=null;
function showMansion(){mansionDraft=mansionDesign(state);openDialog('mansion',mansionDialog(state,mansionDraft,analysis.wealth));}
const flexStyles=document.createElement('link');flexStyles.rel='stylesheet';flexStyles.href='./city/flex.css';document.head.append(flexStyles);
const mansionStyles=document.createElement('link');mansionStyles.rel='stylesheet';mansionStyles.href='./city/mansion.css';document.head.append(mansionStyles);
const richStyles=document.createElement('link');richStyles.rel='stylesheet';richStyles.href='./city/rich-life.css';document.head.append(richStyles);
const artStyles=document.createElement('link');artStyles.rel='stylesheet';artStyles.href='./city/art.css';document.head.append(artStyles);
const empireStyles=document.createElement('link');empireStyles.rel='stylesheet';empireStyles.href='./city/empire.css';document.head.append(empireStyles);
let growthSnapshot=null,reputationSnapshot=null;
import {createJourneyUI} from './journey-ui.js';
import {chapterProgress,journeyState} from './journey.js';
import {REWARDS,rewardDay,rewardStatus,grantReward} from './rewards.js';
import {createDemoSession,rewardProviderMode} from './reward-provider.js';
let rewardSession=null;
import {ensureBasis} from './investment.js';
import {CAREERS,chooseCareer} from './careers.js';
import {STOCK_TYPES,stockInfo,dividendPerShare} from './stocks.js';
import {listedStocks,indexWeights,investIndexUnits,maxIndexUnits,ipoFate,FEE} from './market.js';
import {buyOnMargin,repayMargin,openShort,closeShort,addPlan,removePlan,ensureBroker,MARGIN_RATE,MARGIN_LIMIT,SHORT_FEE} from './broker.js';
import {portfolioReview} from './market-report.js';
import {MOONSHOT_OUTCOMES,ventureOffering,ventureCompany,ensureVentures,investVenture,ventureSummary} from './venture.js';
import {reputationSummary,ownerActivity,ownerScene,EMPIRE_ASSETS,ensureEmpire,empireDialog,empireSummary,acquireEmpireAsset} from './empire.js';
import {acquisitionsDialog,buyCompany,sellCompany} from './acquisitions.js';
import {startShift,resolveOrder,ordersFor} from './activity.js';
import {PATHS,projectQuote,startProject,archiveProject} from './projects.js';
import {createAudio} from './audio.js';
const audio=createAudio();
import {TYPES,STOCKS,MILESTONES,analyze,build,sellAsset,demolishAsset,upgrade,installBillboard,trade,setPlan,tick,chooseEvent,eventShortfall,validSave,migrateSave,coords,landPrice,location,assetValue,canBuild,parcelQuote,buyParcel,buildingAnchor,developmentImpacts,buildingName,expandMap,useMap,index,mapOffset} from './engine.js';
import {createRenderer} from './render.js';
import {createRichGame as createGame,RICH_GOALS,lifestyleDialog,experienceScene,enjoyExperience} from './rich-life.js';
const $=s=>document.querySelector(s),money=n=>'₲'+Math.round(n).toLocaleString('en-US'),esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let state=createGame(),loadWarning='',resumed=false;
try{const raw=sessionSave(localStorage,devSession);if(raw){const saved=migrateSave(JSON.parse(raw));if(validSave(saved)){state=saved;resumed=true;}else loadWarning=L('Could not read the save. Your existing save will not be overwritten.');}}catch{loadWarning=L('Auto-save is unavailable. Use file export instead.');}
ensureBasis(state);
ensureVentures(state);
ensureEmpire(state);
let buildFootprint={width:1,height:1};
useMap(state);
let analysis=analyze(state),speed=0,tool='inspect',tab='property',panel='overview',mapLayer='normal',groundView=false,selected=-1,elapsed=0,last=0,modal=null,saveBlocked=!!loadWarning;
$('#app').innerHTML=L`
<header class="topbar"><a class="brand" href="./"><span class="brand-icon">▥</span><span>SUPER RICH<small>GROW WEALTH. LIVE WELL.</small></span></a><span class="top-divider"></span><button id="city-name" class="city-name" title="Rename"></button><span id="mode-badge" class="badge"></span><div class="top-actions"><span class="version-tag" title="Game version">${VERSION}</span><span id="save-state">Auto-saved locally</span><button data-action="feedback" class="icon-button" aria-label="Send feedback" title="Send feedback">✉</button><button data-action="sound" class="icon-button" aria-label="Turn sound on" title="Turn sound on">♪</button><button data-action="guide" class="icon-button" aria-label="Play guide">?</button><button data-action="new-game" class="new-game-button" title="Start a completely new game">↺<span> New Game</span></button><button data-action="settings" class="icon-button" aria-label="Game settings">⚙</button></div></header>
<main class="game"><section class="world-wrap" aria-label="Business and property map"><canvas id="world" aria-label="Click a lot to sign a contract, or pick one of your own places to run it."></canvas>
<div class="world-title"><span class="eyebrow">YOUR WEALTH. YOUR WORLD.</span><h1>Already Super Rich. Now, how will you live?</h1><p>Bigger assets. A mansion in your taste. A richer today.</p></div>
<div class="world-top"><div class="date-card"><span class="season-dot"></span><span id="date"></span><span id="running" class="muted"></span></div><div class="speed-group"><button data-speed="0" aria-label="Pause">Ⅱ</button><button data-speed="1" aria-label="1× speed">▶</button><button data-speed="2" aria-label="2× speed">2×</button><button data-speed="3" aria-label="3× speed">3×</button><button data-action="step" title="Advance one month" aria-label="Advance one month">↦</button></div></div>
<div id="live-card" class="live-card"></div><div class="month-progress"><i id="month-progress-fill"></i><span id="phase-label">☀ Morning · Opening up</span></div><div class="map-tools"><button data-action="inspect" title="Select buildings and lots · Drag to move the map (Esc)" aria-label="Select buildings and lots"><i class="tool-icon"></i><span>Select</span></button><span></span><button data-action="zoom-in" title="Zoom in" aria-label="Zoom in">+</button><button data-action="zoom-out" title="Zoom out" aria-label="Zoom out">−</button><button data-action="center" title="Reset the map to the whole neighborhood" aria-label="Reset map view"><i class="tool-icon"></i><span>Reset</span></button></div>
<div class="layer-control"><span>Map view</span><select id="layer" aria-label="Map data view"><option value="normal">Neighborhood</option><option value="assets">Highlight my assets</option><option value="pollution">Foot traffic</option><option value="value">Land price</option></select><button id="asset-layer-toggle" data-action="asset-layer" aria-pressed="false">▥ My assets only <b id="asset-map-count">0</b></button><button id="ground-view-toggle" class="asset-layer-toggle" data-action="ground-view" aria-pressed="false" title="Hide buildings to select the lots behind them">▱ Hide buildings</button><div id="legend" hidden><i></i>Low <span>High</span></div><div id="asset-legend" hidden><b><i></i> Gold name tags are your assets</b><span>Tap a building to run it</span></div></div>
<div id="tile-tip" class="tile-tip" hidden></div><div id="world-hint" class="world-hint"></div>
<div class="build-dock"><div class="dock-heading"><div class="dock-tabs"><button data-tab="business">Business</button><button data-tab="property">Property</button><button data-tab="creative">Hobby · Creative</button></div><div class="dock-utilities"><button data-action="portfolio">▥ <span>My Assets</span></button><button data-action="stocks">↗ <span>Stocks</span></button></div></div><div id="build-items"></div><div id="tool-description"></div></div>
</section><aside class="sidebar"><div class="side-heading"><span class="eyebrow">MY WEALTH, MY CHOICES</span><h2>My Wealth Story <span class="live-dot"></span></h2></div><section id="stats"></section><section id="wealth-growth" aria-label="My wealth growth"></section><div class="side-tabs"><button data-panel="overview" class="active">Assets</button><button data-panel="plan">Time & Life</button><button data-panel="budget">Cash Flow</button></div><div id="side-content"></div><div class="sidebar-bottom"><span>Super Rich, my own way. · ${VERSION}</span><button data-action="feedback">Send Feedback ↗</button></div></aside></main>
<footer class="newsbar"><span class="news-tag">MY JOURNEY</span><span id="news"></span><button data-action="history">Growth Log ↗</button></footer><dialog id="dialog"></dialog><input type="file" id="import-file" accept="application/json,.json" hidden>`;
// The switch shows the other language in its own script so a reader of either language can find it.
$('.top-actions [data-action="feedback"]').insertAdjacentHTML('beforebegin',`<button data-action="shortcut" data-shortcut-button class="shortcut-button" title="${L('Add a shortcut to your desktop')}" ${shortcutAdded()?'hidden':''}>⤓<span> ${L('Add Shortcut')}</span></button><button data-action="language" data-lang="${lang==='ko'?'en':'ko'}" class="icon-button lang-button" aria-label="${L('Change language')}" title="${L('Change language')}">${lang==='ko'?'EN':'한'}</button>`);
const renderer=createRenderer($('#world'),()=>state,()=>analysis,interact,i=>{const tip=$('#tile-tip');if(i===MANSION_HIT){tip.hidden=false;tip.textContent=L('🏛 My mansion · Tap to manage or sell');return;}if(i<0){tip.hidden=true;return;}const t=state.tiles[i],{x,y}=coords(i);tip.hidden=false;tip.textContent=`${t.type?buildingName(t):t.terrain==='water'?L('Riverside River'):t.terrain==='mountain'?L('Mountain'):L('Empty lot')} · ${x+1}, ${y+1}${t.owner==='player'?L(' · My asset'):t.owner==='rival'?L(' · ⚑ Rival asset'):state.rival?.target===i?L(' · ⚑ Rival buying soon'):''}`;});renderer.home();
if(devSession.enabled){
 const button=document.createElement('button');button.dataset.action='developer';button.textContent=L('Developer Test');
 $('.top-actions').prepend(button);
}
const journeyUI=createJourneyUI({getState:()=>state,openDialog,changed,toast,renderer,audio});
const landmarkUI=createLandmarkUI({getState:()=>state,openDialog,closeDialog,changed,toast,renderer,preparePlacement:()=>selectTool('inspect'),features:FEATURES});
const cloudStorage={get:k=>{try{return localStorage.getItem(k);}catch{return null;}},set:(k,v)=>{try{v==null?localStorage.removeItem(k):localStorage.setItem(k,v);}catch{}}};
const cloudUI=FEATURES.cloudSave?createCloudUI({client:cloud,storage:cloudStorage,version:VERSION,getState:()=>state,replaceState:loadSave,openDialog,closeDialog,toast,setStatus:text=>{$('#save-state').textContent=text;},showSettings:()=>settings(),scenarioName:s=>s.mode==='sandbox'?'Sandbox':SCENARIOS[s.scenario||'heir']?.name||'-'}):null;
function toast(text){const el=$('#toast');el.textContent=text;clearTimeout(toast.timer);clearTimeout(toast.hideTimer);if(el.showPopover&&!el.matches(':popover-open'))el.showPopover();void el.offsetWidth;el.classList.add('show');toast.timer=setTimeout(()=>{el.classList.remove('show');toast.hideTimer=setTimeout(()=>{if(el.hidePopover&&el.matches(':popover-open'))el.hidePopover();},250);},3000);}
function save(){if(saveBlocked)return;try{const json=JSON.stringify(state);localStorage.setItem(SAVE_KEY,json);$('#save-state').textContent=L('✓ Saved');cloudUI?.afterSave(json);}catch{$('#save-state').textContent=L('Save failed · Export needed');}}
const reputationNotice=document.createElement('div');reputationNotice.id='reputation-notice';reputationNotice.setAttribute('popover','manual');reputationNotice.setAttribute('role','status');reputationNotice.setAttribute('aria-live','polite');document.body.append(reputationNotice);
function refresh(){
 const fame=reputationSummary(state).fame,gain=reputationSnapshot?.state===state?fame-reputationSnapshot.fame:0;
 reputationSnapshot={state,fame};
 if(gain>0)queueMicrotask(()=>{
  reputationNotice.textContent=L`Reputation +${gain.toLocaleString('en-US')}`;
  reputationNotice.hidePopover();($('#dialog').open?$('#dialog'):document.body).append(reputationNotice);reputationNotice.showPopover();clearTimeout(reputationNotice.timer);
  reputationNotice.timer=setTimeout(()=>reputationNotice.hidePopover(),3500);
 });
 if(state.concept==='rich-life')ensureRival(state);useMap(state);analysis=analyze(state);renderHUD();renderSide();
}
function changed(){refresh();save();}
// The mansion estate keeps its place in the original city as the map grows.
function homeTile(){return index(3+mapOffset(state),21+mapOffset(state));}
function developerPanel(){
 if(!devSession.enabled)return;
 speed=0;renderHUD();
 openDialog('developer',`<h2>${L('Developer Test')}</h2><p>${L('Separate test save · Cloud sync off. Your normal game is preserved.')}</p><label class="field-label" for="dev-money">${L('Test cash balance')}<input id="dev-money" type="number" min="0" max="1000000000000" step="1" value="${Math.round(state.money)}"></label><p class="help">${L('Enter a whole number from 0 to 1,000,000,000,000. Peak net worth can increase; lowering cash keeps previous unlocks.')}</p><div class="button-row">${[10000000,100000000,1000000000].map(n=>`<button data-dev-money="${n}">${money(n)}</button>`).join('')}</div><div class="button-row"><button class="primary" data-action="dev-apply">${L('Apply test balance')}</button><button data-action="dev-exit">${L('Return to normal game')}</button></div>`);
}
function loadSave(candidate){state=candidate;ensureBasis(state);saveBlocked=false;selected=-1;renderer.select(-1);speed=0;elapsed=0;closeDialog();selectTool('inspect');changed();renderer.home();}
function renderHUD(){
 let economy=$('#economy-status');if(!economy){economy=document.createElement('div');economy.id='economy-status';$('#wealth-growth').before(economy);}economy.innerHTML=economyBanner(state);
 let flexEntry=$('#flex-entry');if(!flexEntry){flexEntry=document.createElement('button');flexEntry.id='flex-entry';flexEntry.dataset.action='flex';$('#wealth-growth').after(flexEntry);}flexEntry.innerHTML=L`<span>✦ My Flex</span><small>Sports car · Mansion · Yacht ${flexState(state).owned.length}/3 →</small>`;
 $('#wealth-growth').innerHTML=(state.concept==='rich-life'?compoundCard(state):'')+growthCard(state,analysis);
 if(growthSnapshot?.state===state&&Math.round(analysis.wealth)!==growthSnapshot.wealth){const delta=Math.round(analysis.wealth)-growthSnapshot.wealth,card=$('#wealth-growth .wealth-card');card.classList.add(delta>0?'wealth-gained':'wealth-lost');const note=document.createElement('span');note.className='wealth-change';note.textContent=(delta>0?'+':'')+money(delta)+L(' net worth');note.setAttribute('role','status');card.append(note);if(growthModel(state,analysis).tier>growthSnapshot.tier){renderer.burst(analysis.owned[0]?.i??-1,L('New growth stage · ')+growthModel(state,analysis).label);}}
 growthSnapshot={state,wealth:Math.round(analysis.wealth),tier:growthModel(state,analysis).tier};
 renderLive();
 $('#city-name').textContent=localized(state.name)+' ⌄';$('#mode-badge').textContent=state.mode==='sandbox'?L('Sandbox'):L('Super Rich Life · ')+(SCENARIOS[state.scenario||'heir']?.name||L('Heir'));
 $('#date').textContent=L`Year ${1+Math.floor(state.month/12)} · Month ${state.month%12+1}`;$('#running').textContent=state.event?L('Awaiting decision'):speed?L`${speed}× speed`:L('Paused');
 document.querySelectorAll('[data-speed]').forEach(b=>b.classList.toggle('active',Number(b.dataset.speed)===speed));
 document.querySelectorAll('.map-tools [data-action=inspect]').forEach(b=>{b.classList.toggle('active',b.dataset.action===tool);b.setAttribute('aria-pressed',b.dataset.action===tool);});
 $('#stats').innerHTML=L`<div class="treasury"><span>My Net Worth <small>NET WORTH</small></span><strong>${money(analysis.wealth)}</strong><div class="${analysis.net>=0?'positive':'negative'}">${analysis.net>=0?'↗ +':'↘ '}${money(analysis.net)} <span>/ mo projected cash flow</span></div></div><div class="stat-pair"><div><span>Cash on Hand</span><strong>${money(state.money)}</strong><p>${analysis.owned.length} businesses & properties · ${(state.projects||[]).filter(p=>p.status!=='archived').length} creative works</p></div><div><span>Skill / Stress</span><strong>${Math.round(state.skill)}<small>/ ${Math.round(state.stress)}</small></strong><p>${state.stress>65?L('Rest needed · Venture success −15%'):L('A balanced routine')}</p></div></div><div class="economy-strip ${analysis.economy.phase}"><span>Economy <b>${analysis.economy.label}</b> ${analysis.economy.percent>0?'+':''}${analysis.economy.percent}% · ${analysis.economy.trend}${analysis.economy.shock?L` · Shock ${analysis.economy.shock.remaining} mo`:''}</span><span>Reputation <b>${Math.round(reputationSummary(state).fame)}</b></span>${state.concept==='rich-life'?`<span>CH.${chapterOf(state).n} <b>${chapterOf(state).name}</b></span>`:''}</div>`;
 $('#news').textContent=state.log[0]||L('Your story begins.');
 $('#asset-map-count').textContent=analysis.owned.length;$('#world-hint').innerHTML=groundView?L('Buildings hidden · Tap a lot to select it · Press Show Buildings to return'):mapLayer==='assets'?L`${analysis.owned.length} gold name tags are your assets <span>·</span> Tap to see operations`:tool==='inspect'?L('Tap empty land or private buildings to buy <span>·</span> Scroll to zoom <span>·</span> Drag to pan'):L`${TYPES[tool].name} · Select empty land to compare contract terms`;
}
function row(label,value,cls=''){return`<div class="data-row"><span>${label}</span><b class="${cls}">${value}</b></div>`;}
function brokerRows(b){if(!b)return'';return(b.invested?row(L('Savings plan bought'),money(b.invested)):'')+(b.reinvested?row(L('Dividends reinvested'),money(b.reinvested)):'')+(b.shortCost?row(L('Short fees and dividends owed'),'−'+money(b.shortCost)):'');}
function renderSide(){
 if(panel==='overview'&&state.concept==='rich-life'){renderRichOverview();return;}
 if(panel==='plan'&&state.concept==='rich-life'){const at=analysis.attention,art=(state.artCollection?.owned.length||0),curated=(state.plan.curate||0)>=8,diligent=(state.plan.inspect||0)>=20;const slider=(k,n,d)=>L`<label class="tax-label" for="plan-${k}">${n}<b>${state.plan[k]||0}h</b></label><input id="plan-${k}" data-plan="${k}" type="range" min="0" max="${160-Object.entries(state.plan).reduce((a,[key,v])=>a+(key===k?0:v),0)}" step="1" value="${state.plan[k]||0}"><p class="help">${d}</p>`;$('#side-content').innerHTML=L`<section class="side-section"><h3>Owner Time · 160 hours a month</h3><p class="help">Assets earn the money, but the owner pays the attention. Spend no time and assets go cold; spend it all and stress piles up.</p>${slider('manage',L('On-site Management'),L`${at.buildings} managed buildings × ${12}h = ${at.need}h needed · Focus ${Math.round(at.ratio*100)}% · Revenue ×${at.multiplier.toFixed(2)}${at.health<1?L(' (reduced by stress)'):''}`)}${slider('inspect',L('Venture Diligence'),diligent?L('20h or more of diligence · Fixed-multiple venture success +15% active'):L('Assign 20h or more for +15% fixed-multiple venture success'))}${slider('curate',L('Collection Curation'),art?curated?L`Appraisals rising on ${art} owned works · Reputation +2 monthly`:L`Under 8h · Appraisals on your ${art} owned works are stalling`:L('8h or more grows appraisals on owned artwork and adds Reputation +2 monthly'))}${slider('learn',L('Study'),L('₲4 per hour · Skill +0.08'))}${row(L('Rest'),analysis.free+L('h'))}${row(L('Next month stress change'),(()=>{const d=(state.plan.work+state.plan.manage+(state.plan.inspect||0)+(state.plan.curate||0))*.06-analysis.free*.3-3;return (d>0?'+':'')+d.toFixed(1);})())}<div class="button-row"><button data-preset="operator">Operator</button><button data-preset="investor">Investor</button><button data-preset="collector">Collector</button><button data-preset="leisure">Leisure</button></div></section><section class="advisor"><div class="advisor-avatar">☕</div><div><b>${at.ratio<1&&at.buildings?L('Your buildings are waiting on you.'):state.stress>65?L('You are overworked.'):L('Time is an asset too.')}</b><p>${at.ratio<1&&at.buildings?L`On-site management is ${at.need-at.hours}h short, cutting revenue by ${Math.round((1-at.multiplier)*100)}%.`:state.stress>65?L('Past 65 stress, revenue and venture success both drop. Recover with rest or an experience.'):L('The more buildings you own, the more hours they need. Cover them all, or let some go and rest: the owner decides.')}</p></div></section>`;return;}
 if(panel==='plan'){$('#side-content').innerHTML=L`<section class="side-section"><h3>160 Hours a Month, Your Call</h3><button data-action="careers" class="portfolio-link">${analysis.career.job.name} · Choose a job →</button><p class="help">Leftover hours become rest. Running many businesses alone lowers management efficiency.</p>${[['work',L('Work'),L('₲')+analysis.career.hourly.toFixed(2)+L(' per hour · Rises with skill')],['manage',L('Business Ops'),L('28h a month per business is the baseline')],['learn',L('Study'),L('₲4 per hour · Skill +0.08')],['create',L('Create & Produce'),L('Split evenly across active Idea Workshop projects')]].map(([k,n,d])=>L`<label class="tax-label" for="plan-${k}">${n}<b>${state.plan[k]||0}h</b></label><input id="plan-${k}" data-plan="${k}" type="range" min="0" max="${160-Object.entries(state.plan).reduce((a,[key,v])=>a+(key===k?0:v),0)}" step="1" value="${state.plan[k]||0}"><p class="help">${d}</p>`).join('')}${row(L('Rest'),analysis.free+L('h'))}${row(L('Next month skill gain'),'+'+(state.plan.learn*.08).toFixed(1))}<div class="button-row"><button data-preset="worker">Employee</button><button data-preset="owner">Owner</button><button data-preset="creator">Creator</button><button data-preset="balance">Balanced</button></div></section><section class="advisor"><div class="advisor-avatar">☕</div><div><b>Time is an investable asset.</b><p>Work makes cash, creating makes works and royalties, study makes higher pay. Reassign the hours from finished projects to your next move. Past 65 stress, income and revenue fall.</p></div></section>`;return;}
 if(panel==='budget'){$('#side-content').innerHTML=L`<section class="side-section"><h3>Next Month Projected Cash Flow</h3>${row(analysis.career.job.name+L(' pay'),'+'+money(analysis.wage))}${row(L('Stock dividends'),'+'+money(analysis.investment.dividends))}${analysis.empire.income?row(L('Company · Media · Club dividends'),'+'+money(analysis.empire.income)):''}${row(L('Creative · Sales · Royalty income'),'+'+money(analysis.creative.income))}${row(L('Business & rental revenue'),'+'+money(analysis.revenue))}${row(L('Business costs'),'−'+money(analysis.expense))}${row(L('Base living costs'),'−'+money(analysis.lifestyleCosts.base))}${analysis.lifestyleCosts.mansion?row(L('Mansion upkeep'),'−'+money(analysis.lifestyleCosts.mansion)):''}${analysis.lifestyleCosts.sportscar?row(L('Sports car upkeep'),'−'+money(analysis.lifestyleCosts.sportscar)):''}${analysis.lifestyleCosts.yacht?row(L('Yacht upkeep'),'−'+money(analysis.lifestyleCosts.yacht)):''}${analysis.lifestyleCosts.art?row(L('Art insurance & storage'),'−'+money(analysis.lifestyleCosts.art)):''}${row(L('Study costs'),'−'+money(analysis.tuition))}${row(L('Loan interest'),'−'+money(analysis.interest))}${analysis.bonus?row(L('Opportunity income'),'+'+money(analysis.bonus)):''}<div class="total">${row(L('Projected net profit'),money(analysis.net))}</div><p class="help">Total living costs ${money(analysis.living)} = base ${money(analysis.lifestyleCosts.base)} + asset upkeep ${money(analysis.lifestyleCosts.maintenance)}. Settled against the projection made at the start of the month.</p></section><section class="side-section"><h3>Business Loan</h3><p class="help">Loan balance ${money(state.debt)} · 1.2% monthly interest<br>Up to ₲20,000. Borrowed money does not raise net worth.</p><div class="button-row"><button data-action="borrow" ${state.debt>=20000?'disabled':''}>Borrow ₲5,000</button><button data-action="repay" ${state.debt===0||state.money<Math.min(5000,state.debt)?'disabled':''}>Repay ₲5,000</button></div></section><section class="side-section"><h3>Last Month Settlement</h3>${state.lastReport?row(L`Month ${state.lastReport.month} realized cash flow`,money(state.lastReport.net))+brokerRows(state.lastReport.broker):L('<p class="help">Advance a month to see the actual settlement.</p>')}</section>`;return;}
 const next=(state.concept==='rich-life'?RICH_GOALS:MILESTONES).find(m=>!state.milestones.includes(m.wealth)),alerts=[];
 if((state.projects||[]).some(p=>p.status==='working'&&!PATHS[p.type].richOnly)&&(state.plan.create||0)===0)alerts.push(L('A project in production has stalled. Assign creative hours in Time & Life.'));if(analysis.net<0)alerts.push(L('Projected cash flow is in the red. Adjust prices and staff at loss-making businesses, or work more hours.'));if(state.stress>65)alerts.push(L('Stress is cutting your income. Add more rest in Time & Life.'));if(analysis.businessCount*28>state.plan.manage)alerts.push(L('Your operating hours are short for this many businesses. Secure hours before expanding.'));
 $('#side-content').innerHTML=L`<section class="side-section"><h3>Next Wealth Goal <span>✧</span></h3>${next?`<div class="milestone-title"><b>${next.name}</b><span>${money(next.wealth)}</span></div><div class="meter milestone"><i style="width:${Math.max(0,Math.min(100,analysis.wealth/next.wealth*100))}%"></i></div><p class="help">${next.wealth===50000?L('Reaching it unlocks rental apartments'):L('Net worth = cash + sellable assets and inventory + shares − debt')}</p>`:L('<b>Super Rich achieved!</b><p class="help">Keep investing past the goal and finish your own map of assets.</p>')}</section><section class="side-section"><h3>My Portfolio <span>▥</span></h3>${row(L('Business & property sale value'),money(analysis.assets))}${row(L('Content & royalty monthly income'),money(analysis.creative.royalties))}${row(L('Trading inventory value'),money(analysis.inventory))}${row(L('Stock market value'),money(analysis.stocks))}${row(L('Unrealized stock P&L'),money(analysis.investment.unrealized))}${row(L('Loan balance'),'−'+money(state.debt))}${row(L('Property monthly net income'),money(analysis.passive))}<button data-action="portfolio" class="portfolio-link">Manage my ${analysis.owned.length} assets →</button></section><section class="side-section"><h3>Opportunity of the Day <span>↗</span></h3><div class="opportunity"><span class="opportunity-icon">♨</span><div><b>${analysis.owned.length?L('Where to invest next?'):L('My first shop')}</b><p>Cafe fit-out ${money(2400)} + deposit<br>Check foot traffic in dense residential areas.</p></div></div></section><section class="advisor"><div class="advisor-avatar">♧</div><div><b>${alerts.length?L('Advice from your wealth mentor'):L('Start small and grow it yourself.')}</b><p>${alerts[0]||L('Even with no starting capital you can turn your experience into content or take on problem-solving jobs. Pick the path that suits you under Hobby · Creative.')}</p></div></section>`;
}
function renderRichDock(){
 const card=(attr,icon,name,detail,active=false,disabled=false,imageType='')=>`<button class="build-item ${active?'active':''} ${imageType?'illustrated-build':''}" ${attr} ${disabled?'disabled':''}><span class="building-icon" style="--swatch:#d6b16c">${propertyArt(imageType,true)||icon}</span><span><b>${name}</b><small>${detail}</small></span></button>`;
 document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));
 let cards=[],description='';
 if(tab==='business'){
  cards.push(card('data-action="startups"','✧',L('Build my own business'),L('Industry · Concept · Capital · Company exit')));
  cards.push(card('data-action="acquisitions"','▦',L('Buy & Sell Small Companies'),L('From ₲30,000 · 75% steady growth')));
  cards.push(card('data-action="ventures"','✦',L('Venture Portfolio'),L('Pick the odds and the stake')));
  for(const d of Object.values(EMPIRE_ASSETS))cards.push(card('data-action="owners"',d.icon,d.role,L`Acquire ${money(d.cost)} · Unlock ${money(d.unlock)}`));
  description=L('Back a venture, or become the owner of a company, a broadcaster or a sports club. Check the terms and monthly dividends before you sign.');
 }else if(tab==='property'){
  for(const id of ['hotel','office','resort','golf','citypark','housing','hospital','themepark','hq','monument']){const d=TYPES[id],locked=state.mode!=='sandbox'&&(d.unlock||0)>state.highestWealth;cards.push(card(`data-build="${id}"`,d.icon,d.name,locked?L`Unlocks at peak net worth ${money(d.unlock)}`:id==='citypark'?L`Base ${money(d.cost)} + land · No rent · Lifts nearby traffic`:id==='hospital'?L`Base ${money(d.cost)} + land · Clinic → Hospital → General Hospital`:L`Base ${money(d.cost)} + land · Returns vary by location`,tool===id,locked,id));}
  cards.push(card('data-action="mansion-design"','🏛',L('Mansion Design'),L('Size · Floors · Custom spaces'),false,false,'estate'));
  cards.push(card('data-action="portfolio"','▥',L('Manage My Property'),L('Rentals · Expansion · Sale')));
  description=TYPES[tool]?.group==='property'?TYPES[tool].desc+L(' Select empty land to see the total contract cost.'):L('Pick a hotel, resort, tower or a park, housing complex or theme park for residents, then build it on empty land. Operating profit settles automatically every month.');
 }else{
  cards.push(card('data-action="art"','◈',L('World Masterpiece Collection'),L('Masterpiece auctions · View your holdings')));
  cards.push(card('data-action="reputation"','✧',L('Reputation · Giving'),L('Donations · One press interview a month')));
  cards.push(card('data-action="lifestyle"','✈',L('Private Life'),L('Getaways · Art salons · Cruises')));
  for(const [id,d] of Object.entries(PATHS).filter(([,p])=>p.richOnly))cards.push(card(`data-project="${id}"`,d.icon,d.name,L`Production ${money(d.cost)} · Rights income`));
  cards.push(card('data-action="projects"','✎',L('My Creative Portfolio'),L('Production progress · Rights management')));
  description=L('Own masterpieces and fund films or design collections. Your crew handles production automatically.');
 }
 if(tab==='property'&&FEATURES.landmarkStudio)cards.unshift(card('data-landmark="open"','✦',L('My Own Landmark'),FEATURES.aiLandmarks?L('AI design · Image upload'):L('3 example designs · Build cost ×100')));
 $('#build-items').innerHTML=cards.join('');$('#tool-description').textContent=description;
}
function renderDock(){if(state.concept==='rich-life'){renderRichDock();return;}if(tab==='creative'){document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));$('#build-items').innerHTML=Object.entries(PATHS).filter(([,p])=>!p.richOnly).map(([k,d])=>`<button class="build-item" data-project="${k}"><span class="building-icon" style="--swatch:#e8bc79">${d.icon}</span><span><b>${d.name}</b><small>${k==='flip'?L('Inspect, repair and resell yourself'):d.cost?money(d.cost)+L(' upfront'):L('Start with no capital')}</small></span></button>`).join('');$('#tool-description').textContent=L('Income built from your own ideas. Choose a format and a name, then invest creative hours.');return;}document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));$('#build-items').innerHTML=Object.entries(TYPES).filter(([,d])=>d.group===tab).map(([k,d])=>{const locked=state.mode!=='sandbox'&&(d.unlock||0)>state.highestWealth;return`<button class="build-item ${tool===k?'active':''} ${propertyArt(k,true)?'illustrated-build':''}" data-build="${k}" ${locked?'disabled':''} title="${d.desc}"><span class="building-icon" style="--swatch:${d.color}">${propertyArt(k,true)||d.icon}</span><span><b>${d.name}</b><small>${locked?L('Net worth ')+money(d.unlock):state.mode==='sandbox'?L('Free build'):(d.managed?L('Base '):'')+money(d.cost)+' + '+(tab==='business'?L('deposit'):L('land'))}</small></span></button>`;}).join('');}
function setMapLayer(value){mapLayer=['normal','assets','pollution','value'].includes(value)?value:'normal';renderer.setLayer(mapLayer);$('#layer').value=mapLayer;$('#legend').hidden=!['pollution','value'].includes(mapLayer);$('#asset-legend').hidden=mapLayer!=='assets';const button=$('#asset-layer-toggle');button.classList.toggle('active',mapLayer==='assets');button.setAttribute('aria-pressed',mapLayer==='assets');renderHUD();}
function selectTool(v){buildFootprint={width:1,height:1};tool=v;renderer.setTool(v);$('#tool-description').textContent=TYPES[v]?.desc||L('Pick a business and click empty land. You can check location and cost before signing.');renderDock();renderHUD();}
function openDialog(type,html){if(type!=='reward-preview'&&rewardSession){rewardSession.cancel();rewardSession=null;}modal=type;$('#dialog').classList.toggle('mansion-dialog',type==='mansion');$('#dialog').classList.toggle('photo-dialog',type==='photo');$('#dialog').classList.toggle('art-view-dialog',type==='art-view');$('#dialog').classList.toggle('journey-dialog',type==='journey'||type==='growth'||type==='ventures');$('#dialog').innerHTML=L`<button class="dialog-close icon-button" data-action="close" aria-label="Close">×</button>${html}`;$('#dialog').scrollTop=0;if(!$('#dialog').open)$('#dialog').showModal();}
function closeDialog(){rewardSession?.cancel();rewardSession=null;modal=null;$('#dialog').close();}
function parcelDialog(i){const q=parcelQuote(state,i),t=state.tiles[i],loc=location(state,i);openDialog('parcel',propertyArt(t.type)+L`<span class="eyebrow">LOCATION MATTERS</span><h2>${t.type?TYPES[t.type].name:L('Land with new potential')}</h2>${row(L('Land price'),money(q?.land??landPrice(state,i)))}${row(L('Foot traffic index'),trafficText(loc.footfall,false)+(loc.attraction?L` · Nearby attractions +${loc.attraction}`:''))}${row(L('Road access'),loc.boulevard?L('Boulevard · Land value +50% · Rent +30%'):loc.access?L('Roadside · Base price · Revenue'):L('Off-road · Buildable · Discounted price and revenue'))}${q?L`${row(L('Facility price'),money(q.construction*q.level))}<button data-action="buy-parcel" class="primary full" ${state.mode!=='sandbox'&&state.money<q.total?'disabled':''}>Buy ${t.type?L('land and facility'):L('empty lot')} · ${money(q.total)}</button><p>Cash on hand ${money(state.money)} · ${state.mode==='sandbox'?L('Sandbox: no purchase cost deducted.'):L('The cost is deducted the moment you sign.')} ${t.type?L('After purchase, operating profit settles every month. Initial sale value of the facility is 70% of the purchase price.'):L('Empty land you buy never auto-develops. You can build whatever you like on it later.')}</p>`:L('<p>Public facilities and roads cannot be bought.</p>')}<p class="help">Each month new facilities appear on empty land depending on location and land price, and roads extend a little as buildings multiply.</p><button data-action="close" class="full">Look Around the Neighborhood</button>`);}
function interact(i){if(modal)return;if(i===MANSION_HIT){openDialog('mansion-owner',mansionOwnerDialog(state));return;}if(landmarkUI.pick(i))return;i=buildingAnchor(state,i);selected=i;renderer.select(i);renderer.visit(i);const t=state.tiles[i];if(t.owner==='player'){if(TYPES[tool]?.group&&t.tenure==='buy'){contract(i);return;}inspectAsset(i);return;}if(tool==='inspect'){if(t.terrain==='land')parcelDialog(i);return;}contract(i);}
function contract(i){if(!TYPES[tool]?.group)return;renderer.setTool(tool,buildFootprint);openDialog('contract',constructionContract(state,i,tool,buildFootprint));}
function inspectAsset(i){if(state.tiles[i].type==='plot'){openDialog('asset',L`<span class="eyebrow">MY LAND</span><h2>My Empty Lot</h2>${row(L('Current land value'),money(assetValue(state,i)))}${row(L('Road access'),location(state,i).boulevard?L('Boulevard · Land value +50% · Rent +30%'):location(state,i).access?L('Roadside · Buildable'):L('Off-road · Buildable at a lower price and revenue'))}<p>You can build here without paying for the land again. Pick a building below.</p><div class="new-options">${['rental','condo','cafe','market','hotel','resort','office','golf','citypark','housing','hospital','themepark','garden'].map(type=>L`<button data-land-build="${type}">Build ${TYPES[type].name}</button>`).join('')}</div><button data-action="sell" class="full">Sell Lot</button>`);return;}if(TYPES[state.tiles[i].type]?.managed||['citypark','housing'].includes(state.tiles[i].type)){openDialog('asset',landmarkPreview(state.tiles[i].landmark)+constructionDialog(state,i,analysis.reports[i]));return;}if(state.tiles[i].type==='atelier'){const r=analysis.reports[i];openDialog('asset',propertyArt('atelier')+L`<span class="eyebrow">MY FIRST WORKSPACE</span><h2>My Workshop</h2><p>Your own space, earned through trading. On-site repair costs −20% · Creative output +15%.</p>${assetSummary(state,i,r)}${row(L('Monthly rent paid'),money(r.lease))}${row(L('Monthly upkeep'),money(r.maintenance))}<p class="help">Selling ends the repair discount and the creative efficiency bonus.</p><div class="button-row"><button data-journey="open">Browse New Finds</button><button data-action="projects">Creative Projects</button><button data-action="sell">Sell Workshop</button></div>`);return;}const t=state.tiles[i],d=TYPES[t.type],r=analysis.reports[i],rental=d.group==='property',cost=Math.round((t.constructionCost??d.cost)*.8*t.level);openDialog('asset',propertyArt(t.type,false,t.level)+L`<span class="eyebrow">MY BUSINESS · LEVEL ${t.level}</span><h2>${d.name} Operations</h2><p>${t.tenure==='lease'?L('Leased'):L('Land owned')} · ${d.desc}</p>${assetSummary(state,i,r)}${t.type!=='garden'?`<label class="tax-label" for="asset-price">${rental?L('Rent level'):L('Product price')} <b>${t.price}%</b></label><input id="asset-price" data-setting="price" type="range" min="60" max="160" step="5" value="${t.price}"><p class="help">${rental?L('Expected occupancy ')+r.occupancy+L('%. Higher rent means more vacancy.'):L('A percentage of the base price. Higher prices bring fewer customers.')}</p>${!rental?L`<div class="field-grid"><label>Product quality<select data-setting="quality"><option value="1" ${t.quality===1?'selected':''}>Basic · 36% cost</option><option value="2" ${t.quality===2?'selected':''}>Improved · 43% cost</option><option value="3" ${t.quality===3?'selected':''}>Premium · 50% cost</option></select></label><label>Staff<select data-setting="staff">${Array.from({length:9},(_,n)=>L`<option value="${n}" ${t.staff===n?'selected':''}>${n} staff · ${money(n*390)}/mo</option>`).join('')}</select></label></div><label class="policy"><span><b>Local ads</b><small>Revenue +18% · ₲150/mo</small></span><input type="checkbox" data-setting="marketing" ${t.marketing?'checked':''}></label>${row(L('My operating efficiency'),r.manage+'%')}${row(L('Same-sector competition'),r.loc.competition+L(' nearby'))}`:''}`:''}<details><summary>Cost breakdown and asset value</summary>${row(L('Cost of goods'),money(r.goods))}${row(L('Staff wages'),money(r.wages))}${row(L('Space rent'),money(r.lease))}${row(L('Upkeep'),money(r.maintenance))}${row(L('Current sale value'),money(assetValue(state,i)))}</details><div class="button-row"><button data-action="upgrade" ${t.level>=3?'disabled':''}>${t.level>=3?L('Fully expanded'):L('Expand business · ')+money(cost)}</button><button data-action="sell">Sell Asset</button></div>`);}
function portfolio(){openDialog('portfolio',L`<span class="eyebrow">THINGS I BUILT</span><h2>My Businesses and Property</h2><p>Try a different strategy at each place, then double down on whatever earns.</p><div class="new-options">${analysis.owned.map(({t,i})=>L`<button data-asset="${i}"><b>${buildingName(t)} · Lv.${t.level} <span>${money(analysis.reports[i].profit)}/mo</span></b><small>${t.tenure==='buy'?L('Land owned'):L('Leased')} · Sale value ${money(assetValue(state,i))} · Tap to manage</small></button>`).join('')||L('<p class="help">No assets yet. Pick a spot on the map for your first business.</p>')}</div>`);}
// Broker widgets in the stock dialog: per-card margin, savings plan and short controls, the index plan row,
// and the collapsed portfolio review. Company names come from the data tables, so they are already localized.
function brokerRow(id,held,short,plan){return L`<div class="broker-row"><label class="margin-toggle"><input type="checkbox" id="margin-${id}" ${short?'disabled':''}> Buy on margin</label><input id="plan-${id}" type="number" min="1000" step="1000" value="${plan?plan.amount:10000}" aria-label="Monthly amount"><button data-plan="${id}">${plan?L('Change plan'):L('Save monthly')}</button>${plan?L`<button data-plan-stop="${id}">Stop</button>`:''}<button data-short="${id}" ${held>0?'disabled':''}>Short</button></div>`;}
function shortBlock(id,short){if(!short)return'';const pnl=(short.entry-state.prices[id])*short.qty;return L`<div class="short-position"><b>Short ${short.qty} sh · entry ₲${short.entry.toFixed(2)}</b><span class="${pnl>=0?'positive':'negative'}">${money(pnl)}</span><button data-cover="${id}">Cover</button></div>`;}
function planRow(m){const plan=m.plans.find(p=>p.target==='index');return L`<div class="broker-row"><input id="plan-index" type="number" min="1000" step="1000" value="${plan?plan.amount:50000}" aria-label="Monthly amount"><button data-plan="index">${plan?L('Change plan'):L('Save monthly')}</button>${plan?L('<button data-plan-stop="index">Stop</button>'):''}<label class="margin-toggle"><input type="checkbox" data-reinvest ${m.reinvest?'checked':''}> Reinvest dividends into the index</label></div>`;}
function reviewSection(){
 const r=portfolioReview(state),labels={safe:L('Safe','risk'),moderate:L('Moderate','risk'),high:L('High','risk'),critical:L('Near liquidation','risk')};
 const mix=r.byType.map(t=>`<span><i style="width:${(t.weight*100).toFixed(1)}%"></i><b>${STOCK_TYPES[t.type].name}</b> ${(t.weight*100).toFixed(1)}%</span>`).join('')||L('<span>No shares held yet.</span>');
 const ranking=r.ranking.length?L`<p class="help">Return on cost: ${r.ranking.map(x=>`${x.name} ${x.return>=0?'+':''}${(x.return*100).toFixed(1)}%`).join(' · ')}</p>`:'';
 const margin=r.margin.debt>0?L`<p class="stock-alert">Margin ₲${Math.round(r.margin.debt).toLocaleString('en-US')} at ${(MARGIN_RATE*100).toFixed(1)}%/mo · Equity ${(r.margin.equityRatio*100).toFixed(0)}% · A ${r.margin.callAt.toFixed(0)}% drop triggers a margin call</p><div class="button-row"><button data-repay="100000">Repay ₲100,000</button><button data-repay="all">Repay all</button></div>`:'';
 const shorts=r.shorts.map(x=>L`<p class="short-line"><b>${x.name}</b> short ${x.qty} sh · ${x.pnl>=0?'+':'−'}₲${Math.round(Math.abs(x.pnl)).toLocaleString('en-US')} · ${Math.round(x.lossRatio*100)}% of collateral lost <button data-cover="${x.id}">Cover</button></p>`).join('');
 return L`<details class="portfolio-review risk-${r.risk}"><summary>Portfolio review · <b>${labels[r.risk]}</b></summary><div class="review-grid"><div><small>Beta (crash sensitivity)</small><b>${r.beta.toFixed(2)}</b></div><div><small>Loss if the market drops 30%</small><b class="negative">−${money(r.crashLoss)}</b></div><div><small>Dividends per year</small><b>${money(r.annualDividend)}</b></div><div><small>Margin loan</small><b>${money(r.margin.debt)}</b></div></div><div class="index-weights">${mix}</div>${ranking}${margin}${shorts}<p class="help">Margin: borrow up to ${Math.round(MARGIN_LIMIT*100)}% of your stock value; below 30% equity the broker sells for you. Shorts: post the proceeds plus the same again as collateral, pay ${(SHORT_FEE*100).toFixed(1)}% a month and any dividends, and are covered for you once the loss passes 70% of the cash you posted.</p></details>`;
}
function stocks(){ensureBroker(state);const m=state.market,listed=listedStocks(state),weights=indexWeights(state),wById=Object.fromEntries(weights.map(w=>[w.id,w])),cap=n=>n>=1e9?'₲'+(n/1e9).toFixed(1)+'B':'₲'+Math.round(n/1e6)+'M',ih=m.indexHistory,indexChange=ih.length>12?(m.index/ih[ih.length-13]-1)*100:null;
 const card=k=>{const id=k.id,w=wById[id],fate=ipoFate(state,id),type=fate&&fate.boom&&state.month>fate.at?'growth':k.type,held=state.holdings[id],distress=m.distress[id],short=m.shorts[id],plan=m.plans.find(p=>p.target===id);return L`<section class="stock-card${distress?' distress':''}"><h3><span class="stock-type type-${type}">${STOCK_TYPES[type].name}</span><span class="stock-name">${k.name}</span><small>₲${state.prices[id].toFixed(2)}</small></h3><p class="stock-meta">${k.sector} · Market cap ${cap(w.cap)} (${(w.weight*100).toFixed(1)}%) · Dividend yield ${(k.yield*100).toFixed(0)}% · ${STOCK_TYPES[type].desc}</p>${distress?L`<p class="stock-alert">⚠ Under administration · Delisted in ${distress} month(s) unless the price recovers above ₲${(m.peak[id]*.4).toFixed(2)}. Delisted shares are worth nothing.</p>`:''}${priceChart(id)}${row(L('Shares held'),held+L(' sh · ')+money(held*state.prices[id]))}${row(L('Cost basis (incl. fees)'),money(analysis.investment.basis[id]||0))}${row(L('Monthly dividend / share'),'₲'+dividendPerShare(state,id).toFixed(2))}<label class="trade-quantity" for="qty-${id}">Quantity<input id="qty-${id}" type="number" min="1" max="10000000" step="1" value="10"></label><div class="button-row"><button data-stock="${id}" data-stock-side="buy" ${short?'disabled':''}>Buy</button><button data-stock-max="${id}">Max</button><button data-stock="${id}" data-stock-side="sell" ${held===0?'disabled':''}>Sell</button><button data-stock="${id}" data-stock-side="all" ${held===0?'disabled':''}>Sell all</button></div>${brokerRow(id,held,short,plan)}${shortBlock(id,short)}</section>`;};
 openDialog('stocks',L`<span class="eyebrow">VIRTUAL MARKET · ${listed.length} LISTED</span><h2>Eleven listings, eleven personalities.</h2><p>Growth, dividend, defensive, cyclical and speculative names and an office REIT, plus one new listing every year. Prices trend up over the years and plunge in a crash, and a company whose price collapses is delisted. Gains are realized when you sell, and none of this tracks a real market.</p><div class="investment-summary"><div><small>Portfolio value</small><b>${money(analysis.stocks)}</b></div><div><small>Unrealized P&L</small><b class="${analysis.investment.unrealized>=0?'positive':'negative'}">${money(analysis.investment.unrealized)}</b></div><div><small>Realized P&L to date</small><b>${money(analysis.investment.realized)}</b></div><div><small>Next month dividends</small><b>${money(analysis.investment.dividends)}</b></div></div>${reviewSection()}<section class="index-fund"><h3>Riverside Index Fund <small>${m.index.toLocaleString('en-US')}${indexChange===null?'':` · 12 mo ${indexChange>=0?'+':''}${indexChange.toFixed(1)}%`}</small></h3>${sparkline(ih.length?ih:[m.index],L('Riverside index, last 36 months'))}<p class="help">One order buys every listed company in proportion to its market cap, so the biggest names get the biggest share. Whole shares only · 0.5% fee.</p><div class="index-weights">${weights.map(w=>`<span><i style="width:${(w.weight*100).toFixed(1)}%"></i><b>${stockInfo(w.id).name}</b> ${(w.weight*100).toFixed(1)}%</span>`).join('')}</div><label class="trade-quantity" for="index-units">Shares<input id="index-units" type="number" min="1" step="1" value="100"></label><p class="help" id="index-estimate"></p><div class="button-row"><button data-index-units="100">100 sh</button><button data-index-units="1000">1,000 sh</button><button data-index-units="10000">10,000 sh</button><button data-index-units="max">Max</button><button class="primary" data-action="buy-index">Buy the index basket</button></div>${planRow(m)}</section>${state.concept==='rich-life'?compoundOption(state):''}${m.nextIpo===null?'':L`<p class="help">Next IPO in ${Math.max(1,m.nextIpo-state.month)} month(s) · A new listing ends in a jackpot or a delisting within a few years.</p>`}${weights.map(w=>card(stockInfo(w.id))).join('')}${m.delisted.length?L`<details><summary>Delisted companies: ${m.delisted.length}</summary>${m.delisted.map(d=>L`<p class="delisted-row"><b>${localized(d.name)}</b><span>Month ${d.month}${d.shares?L` · ${d.shares.toLocaleString('en-US')} sh written off · −${money(d.lost)}`:''}</span></p>`).join('')}</details>`:''}<p class="help">0.5% trading fee · Cash on hand ${money(state.money)}<br>Dividends are a yield on the current price and land as cash each month. Under administration: a price below 30% of its peak starts a 3-month countdown; recovery above 40% cancels it.</p>`);indexEstimate();}
// Index shares cost the index level in ₲ each: show what the typed count spends, fee included.
function indexEstimate(){const el=$('#index-estimate');if(!el)return;const n=Number($('#index-units').value),level=state.market.index,unit=L`1 share = ₲${level.toLocaleString('en-US')} (today's index)`;el.textContent=Number.isInteger(n)&&n>0?L`${unit} · ${n.toLocaleString('en-US')} sh ≈ ${money(n*level*(1+FEE))} incl. 0.5% fee`:unit;}
function ventures(){const offering=ventureOffering(state),v=ventureSummary(state),full=v.active.length>=3;openDialog('ventures',L`<span class="eyebrow">VENTURE ROOM · HIGH RISK</span><h2>Go to zero, or change the game.</h2><p>Your stake leaves immediately and settles in one shot three months later. No early exit, no protected principal.</p><div class="venture-warning"><b>Total loss of principal is possible</b><span>Standard: fixed multiple · Moonshot: from zero up to 1,000×</span></div><p class="venture-offering">Round ${offering.round+1} · 5 companies · New lineup in ${offering.nextMonth-state.month} mo</p><div class="venture-grid">${offering.companies.map(d=>{const id=d.id;return L`<section class="venture-card"><div class="venture-head"><span>${d.icon}</span><div><small>${d.sector}</small><h3>${d.name}</h3></div></div><p>${d.story}</p><div class="venture-odds"><b>${d.variable?L('₲0'):Math.round(d.chance*100)+'%'}</b><span>${d.variable?L('Minimum payout'):L('Jackpot odds')}</span><strong>${d.multiple.toLocaleString('en-US')}×</strong><span>${d.variable?L('Max payout multiple'):L('Success multiple')}</span></div>${d.variable?L`<p><b>Payouts run from ₲0 up to 1,000×</b><br>The higher the multiple, the lower the odds. Multiples are on the payout including principal.</p><details><summary>See odds by multiple</summary>${MOONSHOT_OUTCOMES.map(o=>`<p>${o.multiple?o.multiple.toLocaleString('en-US')+L('×'):L('₲0 · Total loss')}: ${(o.chance*100).toFixed(1)}%</p>`).join('')}</details>`:'' }<label>Stake<select id="venture-amount-${id}"><option value="10000">₲10,000</option><option value="50000">₲50,000</option><option value="100000">₲100,000</option></select></label><button data-venture="${id}" ${full?'disabled':''}>${full?L('All slots full'):L('Start a 3-month bet')}</button></section>`;}).join('')}</div>${v.active.length?L`<h3>Awaiting results · ${v.active.length}/3</h3><div class="venture-active">${v.active.map(i=>L`<div><span>${ventureCompany(i.ventureId).icon}</span><b>${i.name}<small>${money(i.amount)} invested</small></b><strong>D-${i.dueMonth-state.month} mo</strong></div>`).join('')}</div>`:''}${v.history.length?L`<details><summary>Past results: ${v.history.length} · Jackpots ${v.wins} / Losses ${v.losses}</summary><div class="venture-history">${v.history.map(i=>`<p class="${i.success?'win':'loss'}"><span>${i.success?L('Jackpot ')+(i.payout/i.amount)+'×':L('Total loss')}</span><b>${i.name}</b><strong>${i.success?'+'+money(i.payout):'-'+money(i.amount)}</strong></p>`).join('')}</div></details>`:''}<p class="help">You can back several deals in the same month and run up to three at once. All five companies are replaced every three months, and open investments still settle on their original terms. Each outcome is drawn independently and will not change if you save and reopen.<br>Signing above 65 stress lowers fixed-multiple success odds by 15%, and at Reputation 100 or more you recover 20% of the stake even on a failure. Current stress ${Math.round(state.stress)} · Reputation ${Math.round(reputationSummary(state).fame)}.</p>`);}
function ventureResult(items){const total=items.reduce((n,i)=>n+i.payout,0),wins=items.filter(i=>i.success);openDialog('venture-result',L`<div class="venture-result ${wins.length?'jackpot':'bust'}"><span>${wins.length?'✦':'×'}</span><small>VENTURE EXIT</small><h2>${wins.length?L('Jackpot!'):L('Your stake went to zero')}</h2>${items.map(i=>`<p><b>${i.name}</b><strong>${i.success?(i.payout/i.amount).toLocaleString('en-US')+L('× · +')+money(i.payout):'-'+money(i.amount)+L(' · Total loss')}</strong></p>`).join('')}<h3>${wins.length?L('Total payout ')+money(total):L('No luck this time. The next draw is independent.')}</h3><button data-action="ventures" class="primary full">View Venture Portfolio</button></div>`);audio.play(wins.length?'win':'build');renderer.burst(-1,wins.length?'VENTURE JACKPOT!':'VENTURE FAILED',wins.length?'coin':'loss');}
// Offers on one of my buildings show which one and where, with a jump to it on the map.
function showEvent(){if(!state.event)return;refreshStartupEvent(state);const e=state.event,i=e.options.map(o=>o.tile).find(Number.isInteger),t=state.tiles[i],at=t?.owner==='player'?coords(i):null;openDialog('event',L`<span class="eyebrow">LIFE IS A SERIES OF CHOICES</span><h2>${esc(e.title)}</h2>${e.text.split('\n').map(t=>`<p>${esc(t)}</p>`).join('')}${at?L`<div class="event-target">${row(L('Building'),buildingName(t)+' · Lv.'+t.level)}${row(L('Location'),L`Street ${at.x+1}, Lot ${at.y+1}`)}${row(L('Market value'),money(assetValue(state,i)))}<button class="portfolio-link" data-event-tile="${i}">View on Map →</button></div>`:''}<div class="event-options">${e.options.map((o,i)=>`<button data-choice="${i}"><b>${esc(o.label)} <span>↗</span></b><small>${esc(o.desc)}</small></button>`).join('')}</div>${eventShortfall(state)?L('<p class="help">None of these options fit your cash. Your pick still goes through: the shortfall plus a 10% late fee becomes debt at 1.2% monthly interest, repaid automatically from up to half of each month-end cash balance. To avoid it, close this and sell stocks or buildings first.</p>'):''}<p class="help">Time stays paused until you decide.</p>`);}
function guide(){openDialog('guide',L`<span class="eyebrow">ALREADY RICH. MAKE IT YOURS.</span><h2>Grow the money, enjoy the life.</h2><p>You start at ₲2,000,000 net worth with a stock portfolio, three rental properties, a mansion and a sports car. Work hours are zero: rent and dividends make your cash flow.</p><ol class="guide-list"><li><b>Let the money work</b><span>Buy the index fund or pick from ten stocks with different personalities, and tune rent against vacancy. Sell what you hold or add new property whenever you like.</span></li><li><b>Live it your way</b><span>Design the size, floors, rooms and style of your mansion, then enjoy travel, art and cruises. Every experience is logged as a cost and a memory.</span></li><li><b>It is only fun if you can lose</b><span>The economy swings between boom and bust, moving rent, lodging and compound returns together. Every four months a decision puts your net worth on the line, and if month-end cash goes negative your assets sell at a discount.</span></li><li><b>Beat the rival, open the next chapter</b><span>A rival tycoon goes after the best land first. Peak net worth of ₲10M, ₲30M and ₲150M unlocks resorts, HQ towers and landmarks, and at year 10 or ₲300M the legacy ending scores your run.</span></li><li><b>Reputation is your second currency</b><span>Parties, collections, donations and signature buildings build it, and 3% fades every month. It drives your revenue premium, auction access and what you recover when a venture fails.</span></li></ol><p>▶ 12 seconds = one month · ↦ settle a month · Time pauses while a dialog is open</p><button data-action="close" class="primary full">My Super Rich Life →</button>`);}
const account=createAuth({config:FIREBASE_CONFIG,language:lang,onChange:snap=>{const section=document.querySelector('#account-settings');if(section)section.innerHTML=accountHTML(snap,!!cloudUI);syncAccount(snap);}});
// Signed-in players continue one cloud save on every device. Signing out stops syncing but keeps this device's game.
let accountUid;
function syncAccount({ready,user}){if(!cloudUI||!ready)return;const uid=user?.uid||null;if(uid===accountUid)return;accountUid=uid;if(user)cloudUI.signIn(accountStore({projectId:FIREBASE_CONFIG.projectId,uid,token:()=>user.getIdToken()}));else cloudUI.signOut();}
document.addEventListener('change',e=>{if(e.target?.id==='new-game-confirm')$('#new-game-start').disabled=!e.target.checked;});
document.addEventListener('change',e=>{const c=e.target;if(!c?.matches?.('[data-reinvest]'))return;ensureBroker(state).reinvest=c.checked;changed();toast(c.checked?L('Dividends will be reinvested into the index'):L('Dividend reinvestment off'));});
document.addEventListener('click',e=>{const b=e.target.closest('[data-auth]');if(!b)return;e.preventDefault();if(b.dataset.auth==='google')account.signIn();else if(b.dataset.auth==='sign-out')account.signOut();else if(b.dataset.auth==='retry')account.start();});
// Shown only when the system asks for reduced motion: the player may turn animation on for this game anyway.
function motionSettings(){if(!motion.systemReduced())return'';const link=/Windows/.test(navigator.userAgent)?L('<a href="ms-settings:easeofaccess-visualeffects">Open Windows animation settings ↗</a>'):'';return(motion.forced()?L('<h3>Motion</h3><p class="help">Your system prefers reduced motion, but animation is turned on for this game.</p><button data-action="motion-off" class="full">Follow the System Setting</button>'):L('<h3>Motion</h3><p class="help">Your system prefers reduced motion, so walkers, cars and effects stay still.</p><button data-action="motion-on" class="full">Turn Animation On</button>'))+link;}
function settings(){openDialog('settings',L`<span class="eyebrow">YOUR LIFE, YOUR RULES</span><h2>Game Settings</h2><label class="field-label">Neighborhood name<input id="name-input" maxlength="24" value="${esc(localized(state.name))}"></label><button data-action="rename" class="primary">Save Name</button><h3>Optional Rewards</h3><button data-action="rewards" class="full">View In-Game Rewards and Stash</button><p class="help">Ad previews are optional. Capital, earnings and rewards cannot be cashed out or transferred.</p><h3>Feedback · ${VERSION}</h3><button data-action="feedback" class="full">Share Your Play Notes ✉</button>${cloudUI?cloudUI.settingsHTML():''}<h3>Save Management</h3><p class="help">Saved automatically in this browser. Export a file to continue on another device.</p><div class="button-row"><button data-action="export">Export</button><button data-action="import">Import</button>${FEATURES.legacyModes?L('<button data-action="legacy-save">Export Old Tycoon Save</button>'):''}</div><h3>Fresh Start</h3><p class="help">A new game replaces your current Super Rich slot. The older Life Tycoon save is kept separately.</p><button data-action="new-game" class="full">↺ Start a Completely New Game</button>${state.ending?L('<button data-action="ending" class="full">Replay My Legacy Ending</button>'):''}`+pwaSettings()+motionSettings()+languageSettings());if(FEATURES.accountLogin)$('#dialog h2').insertAdjacentHTML('afterend',`<section id="account-settings">${accountHTML(account.snapshot(),!!cloudUI)}</section>`);}
function languageSettings(){return`<h3>${lang==='ko'?'언어 · Language':'Language · 언어'}</h3><div class="button-row">${LANGS.map(code=>`<button data-action="language" data-lang="${code}"${code===lang?' class="primary" aria-pressed="true"':''}>${code==='ko'?'한국어':'English'}</button>`).join('')}</div>`;}
function history(){const hs=state.history,max=Math.max(1,...hs.map(h=>h.wealth));openDialog('history',L`<span class="eyebrow">MY WEALTH JOURNEY</span><h2>Growth I Built</h2><p>Net worth over the last ${hs.length} months · Now ${money(analysis.wealth)}</p><div class="history-chart">${hs.map(h=>L`<div style="height:${Math.max(3,h.wealth/max*100)}%" title="Month ${h.month}: ${money(h.wealth)}"></div>`).join('')||L('<span>Advance the first month to start the record.</span>')}</div><div class="history-log">${state.log.map(l=>`<p>${esc(l)}</p>`).join('')}</div>`);}
// A popup line for what new development did nearby: land prices and the player's nearby rent.
function developmentNote(e){const parts=[];if(Math.abs(e.land)>=.001)parts.push(L`Nearby land ${e.land>0?'+':'−'}${Math.abs(e.land*100).toFixed(1)}%`);if(e.rent)parts.push(L`My nearby rent ${e.rent>0?'+':'−'}${money(Math.abs(e.rent))}/mo`);return parts.join(' · ');}
function developmentBurst(e){if(Math.abs(e.land)>=.001)renderer.burst(e.tile,L`Land ${e.land>0?'+':'−'}${Math.abs(e.land*100).toFixed(1)}%`,e.land>0?'coin':'loss');}
function month(){if(state.shift){showRush();return;}if(state.event){showEvent();return;}if(modal)return;const n=state.milestones.length,finished=analysis.creative.completions;tick(state);const ventureResults=state.ventures.latest||[];changed();renderDock();renderer.burst(analysis.owned[0]?.i??-1,(state.lastReport.net>=0?'+':'')+money(state.lastReport.net),state.lastReport.net<0?'loss':'coin');audio.play(finished.length?'win':'coin');if(finished.length){const p=state.projects.find(p=>p.id===finished[0]);toast(L`✦ ${p.name} complete! ${p.royalty?L`${money(p.royalty)}/mo from next month`:L`${money(p.sale)} settled`}`);renderer.burst(-1,L('New work complete!'));}if(state.milestones.length>n)toast(L('✦ New wealth goal reached!'));
 const r=state.lastReport;dialogQueue=[];
 if(state.ending&&!state.ending.shown){state.ending.shown=true;save();dialogQueue.push({type:'ending',html:endingDialog(state)});renderer.burst(-1,L`LEGACY · Grade ${state.ending.grade}`,'coin');}
 if(r.chapterUp){dialogQueue.push({type:'chapter',html:chapterDialog(state,r.chapterUp)});renderer.burst(-1,'CHAPTER '+r.chapterUp+' · '+chapterOf(state).name);}
 if(r.fireSale)dialogQueue.push({type:'crisis',html:crisisDialog(r)});
 if(r.broker?.marginCall)toast(L`⚠ Margin call · ${r.broker.marginCall.sold.map(x=>localized(x.name)).join(', ')} sold · ${money(r.broker.marginCall.repaid)} repaid`);
 for(const q of r.broker?.squeezes||[])toast(L`⚠ Short squeeze · ${localized(q.name)} covered · −${money(q.loss)}`);
 if(r.compoundShock)toast('⚠ '+r.compoundShock.label+L(' · Compound assets −')+money(r.compoundShock.loss));
 for(const e of r.rival||[]){if(e.kind==='target'){toast(L('⚑ The rival is eyeing a lot · Buy it first for Reputation +5'));renderer.burst(e.tile,L('⚑ Rival buying soon'),'loss');}else if(e.kind==='bought')renderer.burst(e.tile,L('⚑ Rival broke ground'),'loss');else if(e.kind==='preempted')toast(L('✦ Secured before the rival · Reputation +5'));}
 const nearby=(r.development||[]).filter(e=>developmentNote(e));if(nearby.length){toast(nearby.map(e=>L`${TYPES[e.type].icon||'✦'} ${TYPES[e.type].name} built nearby`+' · '+developmentNote(e)).join(' / '));nearby.forEach(developmentBurst);}
 for(const p of r.pending||[])toast((p.win?'✦ ':'⚠ ')+p.label+' · '+(p.amount>=0?'+':'−')+money(Math.abs(p.amount)));
 if(ventureResults.length)dialogQueue.push({fn:()=>ventureResult(ventureResults)});
 if(state.event)dialogQueue.push({fn:showEvent});
 nextQueued();}
$('#app').addEventListener('click',e=>{
 const b=e.target.closest('button');if(!b||b.disabled)return;
 if(devSession.enabled){
  if(b.dataset.action==='developer'){developerPanel();return;}
  if(b.dataset.devMoney){$('#dev-money').value=b.dataset.devMoney;return;}
  if(b.dataset.action==='dev-apply'){
   if(saveBlocked){toast(L('Could not read the save. Your existing save will not be overwritten.'));return;}
   if(!setTestMoney(state,$('#dev-money').value,true)){toast(L('Enter a whole number from 0 to 1,000,000,000,000.'));return;}
   changed();renderDock();closeDialog();toast(L('Test cash balance applied.'));return;
  }
  if(b.dataset.action==='dev-exit'){const url=new URL(window.location.href);url.searchParams.delete('dev');window.location.assign(url.href);return;}
 }
 if(journeyUI.handle(b))return;
 if(b.dataset.action==='compound'){openDialog('growth',compoundDialog(state));return;}
 if(b.dataset.compoundWithdraw){const r=withdrawCompound(state,b.dataset.compoundWithdraw,b.dataset.amount==='all'?'all':Number(b.dataset.amount));if(r.ok){changed();audio.play('coin');}openDialog('growth',compoundDialog(state));toast(r.msg);return;}
 if(b.dataset.compoundBuy){const r=buyCompound(state,b.dataset.compoundBuy);if(r.ok){changed();audio.play('coin');renderer.burst(-1,r.msg);}openDialog('growth',compoundDialog(state));toast(r.msg);return;}
 if(b.dataset.action==='compound-auto'){const c=ensureCompound(state);c.auto=!c.auto;changed();openDialog('growth',compoundDialog(state));return;}
 if(b.dataset.action==='compound-month'){closeDialog();month();if(!state.event&&!state.shift&&!modal)openDialog('growth',compoundDialog(state));return;}
 if(b.dataset.growthTile!==undefined){const i=Number(b.dataset.growthTile);if(state.tiles[i]?.owner==='player'){closeDialog();renderer.focus(i);renderer.visit(i);selected=i;renderer.select(i);renderer.burst(i,TYPES[state.tiles[i].type].name+L(' · My asset'));}return;}
 if(b.dataset.reward){beginReward(b.dataset.reward);return;}
 if(b.dataset.project){showProject(b.dataset.project);return;}
 if(b.dataset.startProject){const r=startProject(state,b.dataset.startProject,b.dataset.style,$('#project-name').value);toast(r.msg);if(r.ok){changed();showProjects();audio.play('build');}return;}
 if(b.dataset.archiveProject){const id=Number(b.dataset.archiveProject);openDialog('archive-project',L('<h2>Archive this project?</h2><p>This frees a slot. Any remaining subscription and royalty income ends.</p><button class="primary full" data-confirm-archive="')+id+L('">End Income and Archive</button>'));return;}
 if(b.dataset.confirmArchive){const r=archiveProject(state,Number(b.dataset.confirmArchive));changed();showProjects();toast(r.msg);return;}
 if(b.dataset.order){const r=resolveOrder(state,b.dataset.order);if(!r.ok){toast(r.msg);return;}changed();if(r.complete){audio.play('win');renderer.burst(analysis.owned[0]?.i??-1,L('Field pay +')+money(r.reward));openDialog('rush-result',L('<div class="rush-result"><span>✦</span><p>Field rush complete</p><h2>+')+money(r.reward)+L('</h2><p>')+r.served+L(' of 5 orders delivered · The pay has landed in your cash.</p><button data-action="close" class="primary full">On to My Next Chance →</button></div>'));}else{audio.play('coin');showRush();}return;}
 if(b.dataset.build){selectTool(b.dataset.build);return;}if(b.dataset.tab){tab=b.dataset.tab;selectTool('inspect');return;}
 if(b.dataset.panel){panel=b.dataset.panel;document.querySelectorAll('[data-panel]').forEach(x=>x.classList.toggle('active',x===b));renderSide();return;}
 if(b.dataset.speed!==undefined){speed=Number(b.dataset.speed);elapsed=0;renderHUD();return;}
 if(b.dataset.preset){state.plan=({worker:{work:110,manage:0,learn:20},owner:{work:30,manage:90,learn:10,create:0},creator:{work:40,manage:0,learn:20,create:70},balance:{work:70,manage:40,learn:20}})[b.dataset.preset];changed();return;}
 if(b.dataset.redevelop){const footprint=state.tiles[selected].footprint;selectTool(b.dataset.redevelop);buildFootprint=footprint?{...footprint}:{width:1,height:1};contract(selected);return;}
 if(b.dataset.acquireParcel!==undefined){const r=buyParcel(state,Number(b.dataset.acquireParcel));if(r.ok){changed();contract(selected);}toast(r.msg);return;}
 if(b.dataset.landBuild){selectTool(b.dataset.landBuild);contract(selected);return;}
 if(b.dataset.action==='buy-parcel'){const r=buyParcel(state,selected);if(r.ok){changed();inspectAsset(selected);renderer.burst(selected,L('My new lot'));}toast(r.msg);return;}
 if(b.dataset.contract){const before=state.tiles.map(t=>({...t})),r=build(state,selected,tool,b.dataset.contract,b.dataset.contract==='buy'?buildFootprint:undefined),impact=r.ok?developmentImpacts(state,before).find(e=>e.tile===selected):null,note=impact?developmentNote(impact):'';toast(note?r.msg+' · '+note:r.msg);if(note)developmentBurst(impact);if(r.ok){const grown=expandMap(state,selected);if(grown!==selected){selected=grown;renderer.select(selected);toast(L('Map expanded! New land and roads opened beyond the edge.'));}changed();selectTool('inspect');closeDialog();renderer.visit(selected);renderer.burst(selected,L('OPEN! My new place'));audio.play('build');speed=1;renderHUD();}return;}
 if(b.dataset.asset){selected=Number(b.dataset.asset);renderer.select(selected);inspectAsset(selected);return;}
 if(b.dataset.career){const r=chooseCareer(state,b.dataset.career);changed();showCareers();toast(r.msg);return;}
 if(b.dataset.stock){const id=b.dataset.stock,side=b.dataset.stockSide,qty=side==='all'?-state.holdings[id]:Number($('#qty-'+id).value)*(side==='sell'?-1:1);const r=side==='buy'&&$('#margin-'+id)?.checked?buyOnMargin(state,id,qty):trade(state,id,qty);changed();stocks();toast(r.msg);return;}
 if(b.dataset.short){const id=b.dataset.short,r=openShort(state,id,Number($('#qty-'+id).value));if(r.ok)changed();stocks();toast(r.msg);return;}
 if(b.dataset.cover){const r=closeShort(state,b.dataset.cover);if(r.ok){changed();audio.play(r.pnl>=0?'coin':'build');}stocks();toast(r.msg);return;}
 if(b.dataset.plan){const t=b.dataset.plan,r=addPlan(state,t,Number($('#plan-'+t).value));if(r.ok)changed();stocks();toast(r.msg);return;}
 if(b.dataset.planStop){const r=removePlan(state,b.dataset.planStop);if(r.ok)changed();stocks();toast(r.msg);return;}
 if(b.dataset.repay){const r=repayMargin(state,b.dataset.repay==='all'?state.market.margin:Number(b.dataset.repay));if(r.ok)changed();stocks();toast(r.msg);return;}
 if(b.dataset.stockMax){const id=b.dataset.stockMax;$('#qty-'+id).value=Math.max(1,Math.floor(state.money/(state.prices[id]*1.005)));return;}
 if(b.dataset.indexUnits){$('#index-units').value=b.dataset.indexUnits==='max'?Math.max(1,maxIndexUnits(state)):b.dataset.indexUnits;indexEstimate();return;}
 if(b.dataset.action==='buy-index'){const r=investIndexUnits(state,Number($('#index-units').value));if(r.ok){changed();audio.play('coin');}stocks();toast(r.msg);return;}
 if(b.dataset.venture){const amount=Number($('#venture-amount-'+b.dataset.venture).value),r=investVenture(state,b.dataset.venture,amount);if(r.ok){changed();ventures();audio.play('coin');}toast(r.msg);return;}
 if(b.dataset.empireAcquire){const r=acquireEmpireAsset(state,b.dataset.empireAcquire,analysis.wealth);if(r.ok){changed();audio.play('win');renderer.burst(-1,r.msg);}owners();toast(r.msg);return;}
 if(b.dataset.action==='startups'){openDialog('growth',startupsDialog(state));return;}
 if(b.dataset.action==='startup-found'){const r=foundStartup(state,{name:$('#startup-name').value,story:$('#startup-story').value,industry:$('#startup-industry').value,concept:$('#startup-concept').value,amount:$('#startup-amount').value,strategy:$('#startup-strategy').value});if(r.ok){changed();openDialog('growth',startupsDialog(state));}toast(r.msg);return;}
 if(b.dataset.startupSell){const r=sellStartup(state,Number(b.dataset.startupSell));if(r.ok){changed();openDialog('growth',startupsDialog(state));}toast(r.msg);return;}
 if(b.dataset.action==='acquisitions'){openDialog('growth',acquisitionsDialog(state));return;}
 if(b.dataset.companyBuy||b.dataset.companySell){const r=b.dataset.companyBuy?buyCompany(state,b.dataset.companyBuy):sellCompany(state,Number(b.dataset.companySell));if(r.ok){changed();audio.play('win');}openDialog('growth',acquisitionsDialog(state));toast(r.msg);return;}
 if(b.dataset.ownerActivity){const id=b.dataset.ownerActivity,r=ownerActivity(state,id);if(r.ok){changed();audio.play('win');openDialog('growth',ownerScene(state,id));}else owners();toast(r.msg);return;}
 if(b.dataset.propertyParty!==undefined){const i=Number(b.dataset.propertyParty),r=hostPropertyParty(state,i);if(r.ok){changed();audio.play('win');}inspectAsset(i);toast(r.msg);return;}
 if(b.dataset.choice!==undefined){const r=chooseEvent(state,Number(b.dataset.choice));toast(r.msg);if(r.ok){closeDialog();changed();}return;}
 if(b.dataset.action==='new-game'){openDialog('new',newGameDialog(state,analysis.wealth,{cloudLinked:!!cloudUI?.linked()}));return;}
 if(b.dataset.action==='new-game-start'){if(!$('#new-game-confirm')?.checked)return;const name=$('#new-game-name')?.value||'';cloudUI?.stopSync();state=freshGame(name);saveBlocked=false;selected=-1;renderer.select(-1);speed=0;elapsed=0;closeDialog();selectTool('inspect');changed();renderer.home();toast(L('A new story begins.'));return;}
 if(b.dataset.rivalTarget!==undefined){const i=Number(b.dataset.rivalTarget);closeDialog();renderer.focus(i);selected=i;renderer.select(i);renderer.burst(i,L('⚑ Lot the rival is targeting'),'loss');return;}
 if(b.dataset.eventTile!==undefined){const i=Number(b.dataset.eventTile);closeDialog();renderer.focus(i);selected=i;renderer.select(i);renderer.burst(i,L('⚑ The building in the offer'),'loss');toast(L('Press ↦ to return to the offer'));return;}
 if(b.dataset.action==='language'){if(b.dataset.lang!==lang){save();switchLanguage(b.dataset.lang);}return;}
 if(b.dataset.action==='motion-on'||b.dataset.action==='motion-off'){motion.force(b.dataset.action==='motion-on');settings();toast(motion.forced()?L('Animation on. The city moves regardless of your system setting.'):L('Following your system motion setting again.'));return;}
 if(b.dataset.action==='feedback'){openDialog('feedback',feedbackDialog(state,analysis,{viewport:(innerWidth||screen.width)+'×'+(innerHeight||screen.height),cloud:!!cloudUI||devSession.cloudSave,linked:!!cloudUI?.linked()}));return;}
 if(['feedback-copy','feedback-issue','feedback-form','feedback-send'].includes(b.dataset.action)){const v=id=>document.querySelector('#feedback-'+id)?.value||'',text=feedbackText(state,analysis,{bored:v('bored'),stuck:v('stuck'),note:v('note'),replay:v('replay')},{viewport:(innerWidth||screen.width)+'×'+(innerHeight||screen.height)});if(b.dataset.action==='feedback-issue'){window.open(issueURL(text),'_blank','noopener');return;}if(b.dataset.action==='feedback-form'&&FEEDBACK_URL)window.open(FEEDBACK_URL,'_blank','noopener');const manual=()=>{const box=document.querySelector('#feedback-output');if(box){box.hidden=false;box.value=text;box.focus();box.select();}toast(L('Copy the text in the box below.'));};if(b.dataset.action==='feedback-send'){if(!cloudUI&&!devSession.cloudSave)return;b.disabled=true;(cloudUI?cloudUI.sendFeedback(text):cloud.feedback(text,VERSION,null).then(r=>r.ok)).then(sent=>{if(sent){closeDialog();toast(L('Thanks! Your notes reached the developer.'));}else{b.disabled=false;manual();toast(L('Could not send right now. Copy the text below instead.'));}});return;}if(navigator.clipboard?.writeText)navigator.clipboard.writeText(text).then(()=>toast(L('Feedback copied. Paste it into a comment on the post where you found the game.')),manual);else manual();return;}
 if(b.dataset.action==='legacy'){openDialog('growth',legacyDialog(state,analysis));return;}
 if(b.dataset.action==='found'){const r=foundFoundation(state,analysis.wealth);if(r.ok){changed();audio.play('win');renderer.burst(-1,r.msg);}openDialog('growth',legacyDialog(state,analysis));toast(r.msg);return;}
 if(b.dataset.action==='ending'){if(state.ending)openDialog('ending',endingDialog(state));return;}
 const action=b.dataset.action;
 if(action==='reputation'){openDialog('growth',reputationDialog(state));return;}
 if(b.dataset.donate||action==='donate-custom'||action==='media-interview'){const r=action==='media-interview'?mediaInterview(state):donate(state,Number(b.dataset.donate||$('#donation-amount').value));if(r.ok){changed();audio.play('win');}openDialog('growth',reputationDialog(state));toast(r.msg);return;}
 if(b.dataset.vehicleKind){const r=chooseVehicle(state,b.dataset.vehicleKind,b.dataset.vehicleId);if(r.ok)changed();openDialog('growth',vehicleDialog(state,b.dataset.vehicleKind));toast(r.msg);return;}
 if(action==='ground-view'){groundView=!groundView;renderer.setGroundView(groundView);b.textContent=groundView?L('▥ Show buildings'):L('▱ Hide buildings');b.title=groundView?L('Return to the normal building view'):L('Hide buildings to select the lots behind them');b.setAttribute('aria-pressed',groundView);b.classList.toggle('active',groundView);renderHUD();return;}
 if(action==='asset-layer'){setMapLayer(mapLayer==='assets'?'normal':'assets');return;}
 if(action==='cars'||action==='yachts'){openDialog('growth',vehicleDialog(state,action==='cars'?'sportscar':'yacht'));return;}
 if(b.dataset.photo){const html=photoViewer(state,b.dataset.photo);if(html){openDialog('photo',html);}return;}
 if(action==='legacy-save'){const raw=localStorage.getItem('super-rich-life-v2');if(!raw){toast(L('No older Tycoon save in this browser.'));return;}const url=URL.createObjectURL(new Blob([raw],{type:'application/json'})),link=document.createElement('a');link.href=url;link.download='super-rich-legacy-backup.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);return;}
 if(action==='lifestyle'){openDialog('growth',lifestyleDialog(state));return;}
 if(b.dataset.experience){const id=b.dataset.experience,before=state.stress,r=enjoyExperience(state,id);if(r.ok){changed();audio.play('win');renderer.burst(-1,r.msg);}openDialog('growth',r.ok?experienceScene(state,id,before-state.stress):lifestyleDialog(state));toast(r.msg);return;}
 if(b.dataset.artBuy){const r=buyArtwork(state,b.dataset.artBuy);if(r.ok){changed();audio.play('win');renderer.burst(-1,r.msg);}openDialog('growth',artDialog(state));toast(r.msg);return;}
 if(action==='art'){openDialog('growth',artDialog(state));return;}
 if(b.dataset.artView){const html=artViewDialog(state,b.dataset.artView);if(html)openDialog('art-view',html);return;}
 if(b.dataset.artZoom!==undefined){const zoomed=b.closest('.art-room').classList.toggle('zoomed');b.setAttribute('aria-pressed',zoomed);return;}
 if(action==='mansion-design'){showMansion();return;}
 if(action==='mansion-reset'){mansionDraft={...DEFAULT_MANSION};openDialog('mansion',mansionDialog(state,mansionDraft,analysis.wealth));return;}
 if(action==='mansion-build'){if(!mansionDraft)return;const r=buildMansion(state,mansionDraft,analysis.wealth);if(r.ok){changed();closeDialog();renderer.focus(homeTile());renderer.burst(homeTile(),L('MY DESIGN · My mansion is finished'));audio.play('win');}toast(r.msg);return;}
 if(action==='mansion-owner'||action==='mansion-sell'){openDialog('mansion-owner',mansionOwnerDialog(state,action==='mansion-sell'));return;}
 if(action==='confirm-mansion-sell'){const r=sellMansion(state);if(r.ok){changed();closeDialog();renderer.burst(homeTile(),L('SOLD · My mansion'),'coin');audio.play('coin');}toast(r.msg);return;}
 if(b.dataset.flexBuy){const r=buyFlex(state,analysis.wealth,b.dataset.flexBuy);if(r.ok){changed();audio.play('win');renderer.burst(-1,r.msg);}openDialog('growth',flexDialog(state,analysis));toast(r.msg);return;}
 if(action==='flex'){openDialog('growth',flexDialog(state,analysis));return;}
 if(action==='flex-home'){closeDialog();renderer.focus(homeTile());renderer.burst(homeTile(),L('WELCOME HOME · My mansion'));return;}
 if(action==='flex-party'){const r=hostParty(state);if(r.ok){changed();closeDialog();renderer.home();renderer.burst(-1,r.msg);audio.play('win');}toast(r.msg);return;}
 if(action==='growth'){openDialog('growth',growthDialog(state,analysis));return;}if(action==='rewards'){showRewards();return;}if(action==='careers'){showCareers();return;}if(action==='rush'){showRush();return;}if(action==='projects'){showProjects();return;}if(action==='creative-plan'){closeDialog();panel='plan';document.querySelectorAll('[data-panel]').forEach(b=>b.classList.toggle('active',b.dataset.panel===panel));renderSide();$('#side-content').scrollIntoView({behavior:'smooth',block:'nearest'});return;}if(action==='sound'){const on=audio.toggle();b.textContent=on?'♫':'♪';b.setAttribute('aria-label',on?L('Turn sound off'):L('Turn sound on'));b.classList.toggle('active',on);return;}
 if(action==='inspect')selectTool(action);else if(action==='zoom-in')renderer.zoom(.15);else if(action==='zoom-out')renderer.zoom(-.15);else if(action==='center')renderer.home();
 else if(action==='step')month();else if(action==='close'){closeDialog();nextQueued();}else if(action==='guide')guide();else if(action==='shortcut')openDialog('shortcut',shortcutDialog());else if(action==='settings'||b.id==='city-name')settings();else if(action==='history')history();else if(action==='portfolio')portfolio();else if(action==='stocks')stocks();else if(action==='ventures')ventures();else if(action==='owners')owners();
 else if(action==='rename'){state.name=$('#name-input').value.trim()||L('Riverside');changed();toast(L('Name saved.'));}
 else if(action==='borrow'&&state.debt<20000){state.debt+=5000;state.money+=5000;changed();toast(L('Borrowed ₲5,000 · ₲60 monthly interest added'));}
 else if(action==='repay'){const n=Math.min(5000,state.debt);if(state.money>=n){state.money-=n;state.debt-=n;changed();}}
 else if(action==='upgrade'){const r=upgrade(state,selected);changed();if(r.ok){closeDialog();renderer.burst(selected,'LEVEL UP!');audio.play('build');}else inspectAsset(selected);toast(r.msg);}
 else if(action==='billboard'){const r=installBillboard(state,selected);changed();if(r.ok)audio.play('build');inspectAsset(selected);toast(r.msg);}
 else if(action==='demolish'){openDialog('demolish',demolitionDialog(state,selected));}
 else if(action==='confirm-demolish'){const r=demolishAsset(state,selected);if(r.ok){closeDialog();changed();inspectAsset(selected);}toast(r.msg);}
 else if(action==='sell'){openDialog('sell',saleDialog(assetSaleReport(state,selected)));}
 else if(action==='confirm-sell'){const r=sellAsset(state,selected);closeDialog();changed();if(r.ok)openDialog('sale-receipt',saleDialog(r.report,true));toast(r.msg);}
 else if(action==='export'){const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='super-rich-life-'+state.month+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast(L('Save file exported.'));}
 else if(action==='import')$('#import-file').click();
});
$('#app').addEventListener('change',e=>{if(e.target.dataset.footprint){buildFootprint[e.target.dataset.footprint]=Number(e.target.value);contract(selected);return;}if(e.target.dataset.plan){setPlan(state,e.target.dataset.plan,Number(e.target.value));changed();}if(e.target.dataset.setting){const k=e.target.dataset.setting,t=state.tiles[selected];if(t?.owner==='player'){t[k]=k==='marketing'?e.target.checked:Number(e.target.value);changed();inspectAsset(selected);}}if(e.target.id==='layer')setMapLayer(e.target.value);});
$('#app').addEventListener('change',e=>{if(e.target.dataset.footprint){buildFootprint[e.target.dataset.footprint]=Number(e.target.value);contract(selected);return;}const key=e.target.dataset.mansion;if(!key||!mansionDraft)return;const input=e.target;mansionDraft[key]=input.type==='checkbox'?input.checked:['size','floors','rooms','area'].includes(key)?Number(input.value):input.value;if(key==='area')mansionDraft.area=Math.max(120,Math.min(10000,Math.round(mansionDraft.area)||120));if(key==='size'){mansionDraft.area=({1:120,2:200,3:320,4:800,5:2000})[mansionDraft.size];$('[data-mansion="area"]').value=mansionDraft.area;}mansionDraft.rooms=Math.max(2,Math.min(roomCapacity(mansionDraft),Math.round(mansionDraft.rooms)||2));const rooms=$('[data-mansion="rooms"]');rooms.max=roomCapacity(mansionDraft);rooms.value=mansionDraft.rooms;$('#mansion-capacity').textContent=L`2–${roomCapacity(mansionDraft)} at this size`;$('#mansion-preview').innerHTML=mansionPreview(state,mansionDraft,analysis.wealth);document.querySelectorAll('[data-style-thumb]').forEach(img=>img.src='./'+mansionArtURL({...mansionDraft,style:img.dataset.styleThumb},true));});
$('#import-file').addEventListener('change',async e=>{const file=e.target.files[0];if(!file)return;try{if(file.size>4000000)throw Error();const candidate=migrateSave(JSON.parse(await file.text()));if(!validSave(candidate))throw Error();openDialog('import',L`<h2>Load this saved story?</h2><p>${esc(candidate.name)} · ${candidate.month} months in. This replaces your current auto-save.</p><button class="primary" id="confirm-import">Load</button>`);$('#confirm-import').onclick=()=>{loadSave(candidate);toast(L('Save file loaded.'));};}catch{toast(L('Not a valid save file. Your current game is unchanged.'));}finally{e.target.value='';}});
$('#dialog').addEventListener('cancel',()=>{rewardSession?.cancel();rewardSession=null;modal=null;if(dialogQueue.length)setTimeout(nextQueued,0);});
window.addEventListener('keydown',e=>{if(modal||['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName))return;if(e.code==='Space'){e.preventDefault();speed=speed?0:1;elapsed=0;renderHUD();return;}const keys={'1':'cafe','2':'market','3':'studio',Escape:'inspect'};if(keys[e.key]){const v=keys[e.key];if(TYPES[v]?.group)tab=TYPES[v].group;selectTool(v);}});
window.addEventListener('beforeunload',save);
function frame(now){const dt=Math.min((now-last)/1000,.25);last=now;advanceReward(dt);if(!document.hidden&&!modal&&!state.event&&!state.shift&&speed){elapsed+=dt*speed;if(elapsed>=12){elapsed=0;month();}}renderer.render(now,elapsed/12);$('#month-progress-fill').style.width=(elapsed/12*100)+'%';const phase=elapsed<4?L('☀ Morning · Customers arriving'):elapsed<8?L('☀ Afternoon · Open for business'):L('☾ Evening · Closing the month');if($('#phase-label').textContent!==phase)$('#phase-label').textContent=phase;requestAnimationFrame(frame);}
motion.apply();refresh();renderDock();selectTool('inspect');requestAnimationFrame(frame);
if(devSession.enabled)save();
// First visit: point out the shortcut once, waiting until no dialog is open.
try{if(!shortcutAdded()&&!localStorage.getItem(SHORTCUT_HINT_KEY)){
 const hint=document.createElement('aside');hint.id='shortcut-hint';hint.hidden=true;hint.setAttribute('role','status');
 hint.innerHTML=L`<b>⤓ Open SUPER RICH in one click</b><p>Add a shortcut to your desktop or home screen and come back to your empire anytime.</p><div class="button-row"><button class="primary" data-hint="open">Show Me How</button><button data-hint="later">Later</button></div>`;
 hint.addEventListener('click',e=>{const b=e.target.closest('[data-hint]');if(!b)return;hint.hidden=true;if(b.dataset.hint==='open')openDialog('shortcut',shortcutDialog());});
 document.body.append(hint);
 const show=()=>{if(shortcutAdded())return;if(modal){setTimeout(show,5000);return;}try{localStorage.setItem(SHORTCUT_HINT_KEY,'1');}catch{}hint.hidden=false;};
 setTimeout(show,12000);
}}catch{}
try{if(innerWidth>0&&innerWidth<720&&!localStorage.getItem('super-rich-mobile-hint')){localStorage.setItem('super-rich-mobile-hint','1');setTimeout(()=>toast(L('The map has more room on a PC screen. Mobile play works too.')),1500);}if(motion.systemReduced()&&!motion.forced()&&!localStorage.getItem('super-rich-motion-hint')){localStorage.setItem('super-rich-motion-hint','1');setTimeout(()=>toast(L('Animations are off in your system settings. Turn them on in ⚙ Game Settings.')),2500);}}catch{}if(loadWarning)toast(loadWarning);else if(state.shift)showRush();else if(state.event)showEvent();
cloudUI?.open();
if(FEATURES.accountLogin)account.start();
if(resumed&&!loadWarning&&state.month>0)setTimeout(()=>toast(L`Continuing your saved game · Year ${Math.floor(state.month/12)+1}, month ${state.month%12+1}`),800);
function renderLive(){
 if(state.concept==='rich-life'){const v=ventureSummary(state),e=empireSummary(state),art=artPortfolio(state),nextOwner=[20000000,60000000,150000000][e.owned.length];$('#live-card').innerHTML=L`<div class="live-card-title"><span class="live-dot"></span> PRIVATE OFFICE · My wealth, my choices</div><div class="rich-office"><button data-action="legacy" class="legacy-launch"><b>♡ Reputation · Legacy</b><small>Reputation ${Math.round(reputationSummary(state).fame).toLocaleString('en-US')} · ${reputationSummary(state).tier.name} · CH.${chapterOf(state).n}${state.ending?L(' · Ending reached'):''}</small></button><button data-action="startups"><b>✧ Build my own business</b><small>Industry · Concept · Capital · Company exit</small></button><button data-action="ventures" class="venture-launch"><b>✦ Venture Investing</b><small>${v.active.length?L`${v.active.length} in review · Settling soon`:L('From ₲0 up to 1,000×')}</small></button><button data-action="owners" class="owner-launch"><b>♛ Owners Club</b><small>${e.owned.length?L`Reputation ${reputationSummary(state).fame} · ${reputationSummary(state).tier.name}`:L`From ${money(nextOwner)} peak net worth`}</small></button><button data-action="art" class="art-launch"><b>🖼 World Masterpiece Auction</b><small>${art.owned.length?L`${art.owned.length} works · ${money(art.value)}`:L('From the Mona Lisa to Monet · ₲10M+')}</small></button><button data-action="stocks"><b>↗ Stock Investing</b><small>Stocks · Safe account · Dividends ${money(analysis.investment.dividends)}/mo</small></button><button data-action="mansion-design"><b>🏛 Design My Mansion</b><small>Size · Floors · Rooms · Taste</small></button><button data-action="lifestyle"><b>✦ Time to Enjoy It</b><small>Travel · Art · Yacht cruises</small></button></div></div>`;return;}
 const g=chapterProgress(state),j=journeyState(state);
 $('#live-card').innerHTML=L`<div class="live-card-title"><span class="live-dot"></span> CHAPTER 01 · Keys to the first workshop</div><button class="chapter-launch" data-journey="open"><b>${g.done?L('What is the next find worth?'):j.deal?L('A deal I have not closed yet'):L('From a box in storage to a workshop of my own.')}</b><small>${g.done?L('Workshop secured · Keep trading'):Math.min(g.wins,3)+L('/3 profitable deals · ')+money(g.profit)+L(' / ₲350 profit')}</small><span>${j.deal?L('Resume the deal'):g.ready&&!g.done?L('Get my first workshop'):L('Hunt the back streets')} →</span></button><div class="meter"><i style="width:${g.percent}%"></i></div><div class="live-actions"><button data-action="rush"><b>⚡ Field Rush</b><small>Budget materials and deliver orders</small></button><button data-action="projects"><b>✎ Idea Workshop</b><small>Build your own income by creating</small></button></div><button data-action="rewards" class="reward-open"><small>Optional rewards · ${rewardProviderMode(window.location.hostname)==='local-demo'?L('Test mode'):L('Coming soon')} →</small></button>`;
}
function showRush(){
 const r=state.shift;if(!r){const result=startShift(state);if(!result.ok){toast(result.msg);return;}save();}
 const run=state.shift,orders=ordersFor(state),o=orders[run.round],canServe=run.stock>=o.stock&&run.energy>=o.energy,canCraft=state.skill>=15&&run.stock>=o.stock-1&&run.energy>=o.energy+1;
 openDialog('rush',L`<div class="rush-heading"><span class="eyebrow">SIDE QUEST · Field Rush</span><h2>Choosing well beats taking everything.</h2><p>Five side jobs this month. Spend materials and focus to maximize your net pay.</p></div><div class="rush-resources"><span>▦ Materials <b>${run.stock}/${run.stockLimit||6}</b></span><span>ϟ Focus <b>${run.energy}/5</b></span><span>Pay so far <b>${money(run.earned)}</b></span></div><div class="order-queue">${orders.map((q,n)=>L`<div class="${n===run.round?'current':n<run.round?'finished':''}"><span>${n<run.round?(run.results[n].pay?'✓':'—'):q.icon}</span><b>${money(q.pay)}</b><small>Materials ${q.stock} · Focus ${q.energy}</small></div>`).join('')}</div><div class="order-ticket"><span class="ticket-icon">${o.icon}</span><small>ORDER ${run.round+1} / 5</small><h3>${o.name}</h3><p>${o.text}</p><div class="ticket-price">+${money(o.pay)} <small>Net pay, materials excluded</small></div></div><div class="rush-choices"><button data-order="serve" class="primary" ${canServe?'':'disabled'}><b>Accept order</b><small>Materials ${o.stock} · Focus ${o.energy}</small></button><button data-order="craft" ${canCraft?'':'disabled'}><b>Custom build · +₲25</b><small>${state.skill<15?L('Requires skill 15'):L('Materials ')+(o.stock-1)+L(' · Focus ')+(o.energy+1)}</small></button><button data-order="skip"><b>See the next job →</b><small>Save resources for a better one</small></button></div><p class="help">Everything settles once all five jobs are done. One run a month plus any stored tickets · Progress is saved if you close mid-run.</p>`);
}
function showProjects(){const ps=state.projects||[];openDialog('projects',L`${state.concept==='rich-life'?L('<span class="eyebrow">CREATIVE PATRON</span><h2>Turn your taste into a work.</h2><p>Finish films and design collections with a professional crew. Fund production and your crew works automatically each month.</p>'):L('<span class="eyebrow">MAKE SOMETHING. MAKE A LIVING.</span><h2>Where ideas become assets.</h2><p>Turn your ideas and creative hours into income.</p>')}<div class="project-paths">${Object.entries(PATHS).filter(([,p])=>state.concept==='rich-life'?p.richOnly:!p.richOnly).map(([id,p])=>`<button data-project="${id}"><span>${p.icon}</span><b>${p.name}</b><small>${p.tag}</small><em>${id==='flip'?L('Inspect, repair and resell yourself'):p.cost?money(p.cost)+L(' upfront'):L('Start with no capital')}</em></button>`).join('')}</div><h3>My Studio <small>${state.concept==='rich-life'?L('Automatic production'):L`${state.plan.create||0}h assigned this month`}</small></h3>${ps.filter(p=>p.status!=='archived').map(p=>`<div class="project-progress"><div><b>${esc(p.name)}</b><small>${p.status==='working'?(PATHS[p.type].richOnly?L`${Math.ceil((p.hours-p.progress)/analysis.creative.progress[p.id])} mo to completion`:Math.floor(p.progress)+'/'+p.hours+L('h')):p.status==='earning'?money(p.royalty)+L('/mo · ')+p.monthsLeft+L(' mo left'):L('Complete · Paid out')}</small></div><div class="meter"><i style="width:${p.progress/p.hours*100}%"></i></div>${p.status!=='working'?L`<button data-archive-project="${p.id}">Archive Project</button>`:''}</div>`).join('')||L('<p class="help">No projects yet. Pick your first idea.</p>')}${state.concept==='rich-life'?L('<p class="help">Up to 3 in production and 8 owned. Each crew works independently and production advances automatically each month.</p>'):L('<button data-action="creative-plan" class="primary full">Assign Creative Hours →</button><p class="help">Up to 3 in production and 8 owned. Creative hours are split evenly across active work.</p>')}`);}
function showProject(type){if(type==='flip'){journeyUI.show();return;}const p=PATHS[type];openDialog('project-start',L`<span class="eyebrow">${p.tag}</span><h2>${p.icon} ${p.name}</h2><p>${p.desc}</p><label class="field-label">My project name<input id="project-name" maxlength="30" value="${p.name}" placeholder="What idea do you want to make?"></label><div class="project-strategies">${[['quick',p.richOnly?L('Quick production'):L('Quick test'),p.richOnly?L('Earlier release, lower payout'):L('See a first result in fewer hours')],['balanced',p.richOnly?L('Standard production'):L('Balanced build'),p.richOnly?L('Balanced duration and payout'):L('A balance of time and polish')],['craft',p.richOnly?L('Premium production'):L('Crafted finish'),L('Longer to make, higher pay')]].map(([style,name,desc])=>{const q=projectQuote(state,type,style);return L`<button data-start-project="${type}" data-style="${style}"><b>${name} <span>${p.richOnly?L`${Math.ceil(q.hours/analysis.creative.crewHours)} mo automatic`:L`${q.hours}h`}</span></b><small>${desc}<br>Upfront ${money(q.cost)} · Completion pay ${money(q.sale)}${q.royalty?L(' + ')+money(q.royalty)+L('/mo × 24 mo'):''}</small></button>`;}).join('')}</div><p class="help">${p.richOnly?L`A fixed contract priced at skill ${Math.round(state.skill)}. Your crew completes production automatically in the time shown. Royalties start the month after completion.`:L`A fixed contract priced at skill ${Math.round(state.skill)}. Assign hours to finish it and the pay settles automatically. With 0 creative hours nothing progresses.`}</p>`);}
function showCareers(){openDialog('careers',L`<span class="eyebrow">MY TIME HAS VALUE</span><h2>The first capital you earn.</h2><p>You decide your work hours. Study builds skill and opens jobs that pay more per hour.</p><div class="career-current">Current skill <b>${Math.round(state.skill)}</b> · Work this month <b>${state.plan.work}h</b></div><div class="new-options">${Object.entries(CAREERS).map(([id,c])=>L`<button data-career="${id}" ${state.skill<c.skill?'disabled':''}><b>${c.icon} ${c.name} <span>${state.career===id||!state.career&&id==='flexible'?L('Current job'):L`${money(c.hourly+state.skill*.09)} / hr`}</span></b><small>${c.desc}<br>₲${(c.hourly+state.skill*.09).toFixed(2)} / hr · ${state.skill<c.skill?L`Requires skill ${c.skill}`:L('Available')}</small></button>`).join('')}</div><button data-action="creative-plan" class="primary full">Assign Work and Study Hours →</button><p class="help">Pay settles automatically each month. High stress reduces your actual income.</p>`);}
function sparkline(values,label){const lo=Math.min(...values)*.98,hi=Math.max(...values)*1.02;const points=values.map((v,n)=>`${(n/Math.max(1,values.length-1)*280).toFixed(1)},${(48-(v-lo)/(hi-lo||1)*40).toFixed(1)}`).join(' ');return `<svg class="stock-chart" viewBox="0 0 280 55" role="img" aria-label="${label}"><path d="M0 50H280" stroke="#dce5d2"/><polyline points="${points}" fill="none" stroke="#83a569" stroke-width="2.5"/></svg>`;}
function priceChart(id){const values=[stockInfo(id).base,...state.history.map(h=>h.prices?.[id]).filter(Number.isFinite),state.prices[id]].slice(-36);return sparkline(values,L`${stockInfo(id).name} recent price trend`);}
function showRewards(){
 const mode=rewardProviderMode(window.location.hostname),wallet=state.rewards,day=rewardDay(),used=wallet?.day===day?wallet.claims.length:0;
 openDialog('rewards',L`<span class="eyebrow">OPTIONAL BOOST · Optional rewards</span><h2>A small boost for your next chance.</h2><p>Take a reward for in-game capital or an extra field run. Skip them entirely and work, creating, business and investing all still play in full.</p><div class="reward-notice"><b>${mode==='local-demo'?L('Developer reward preview · Not a real ad'):L('Ad service coming soon')}</b><span>${mode==='local-demo'?L('A 5-second preview screen shows how the reward flow works. No ad is served and no ad revenue is earned.'):L('No real ad provider is connected yet. Normal play works as usual.')}</span></div><div class="reward-wallet"><span>Stored materials <b>${wallet?.materials||0}</b></span><span>Extra tickets <b>${wallet?.tickets||0}</b></span><span>Rewards taken today <b>${used}/3</b></span></div><div class="reward-options">${Object.entries(REWARDS).map(([kind,r])=>{const status=rewardStatus(state,kind,day);return`<section><div class="reward-option-title"><span>${r.icon}</span><div><h3>${r.name}</h3><b>${r.label}</b></div></div><p>${r.description}</p><button data-reward="${kind}" ${mode!=='local-demo'||!status.ok?'disabled':''}>${!status.ok?L('Not available right now'):mode==='local-demo'?L`Watch 5s and claim ${r.label}`:L('Ads coming soon')}</button>${!status.ok?`<small>${status.msg}</small>`:''}</section>`;}).join('')}</div><p class="help">Each reward is once a day by your device local date. You can store one material pack and one ticket. Materials apply at the start of your next field run; tickets are used automatically once the monthly run is done.</p><div class="game-only-note">All capital, rewards and investment results are in-game only.<br>They cannot be withdrawn as cash or gift cards, or transferred to anyone.</div><button data-action="close" class="full">Keep Playing Without Rewards</button>`);
}
function beginReward(kind){
 if(rewardSession)return;
 if(rewardProviderMode(window.location.hostname)!=='local-demo'){toast(L('No real ad service is connected yet.'));return;}
 const status=rewardStatus(state,kind);if(!status.ok){toast(status.msg);showRewards();return;}
 const r=REWARDS[kind];rewardSession=createDemoSession({id:crypto.randomUUID(),kind,day:rewardDay()});
 openDialog('reward-preview',L`<span class="eyebrow">TEST PREVIEW · Not a real ad</span><h2>${r.name} reward preview</h2><p>Finish the preview and <b>${r.label}</b> lands in your game. Close it early and nothing is granted, with no penalty to your play.</p><div class="reward-preview-art"><span>${r.icon}</span><b>A small boost for your idea</b><small>A developer preview with no external ad and no ad revenue</small></div><div class="reward-progress"><i id="reward-progress-fill"></i></div><p id="reward-countdown" class="reward-countdown" role="status">5s left</p><button data-action="close" class="full">Skip and Return to the Game</button><p class="help">The preview only advances while this screen is visible. Game time pauses meanwhile.</p>`);
}
function advanceReward(dt){
 if(!rewardSession||modal!=='reward-preview')return;
 rewardSession.advance(dt*1000,!document.hidden);
 const remaining=rewardSession.remaining;
 $('#reward-progress-fill').style.width=((5000-remaining)/50)+'%';
 const label=Math.ceil(remaining/1000)+L('s left');if($('#reward-countdown').textContent!==label)$('#reward-countdown').textContent=label;
 const receipt=rewardSession.complete();if(!receipt)return;rewardSession=null;
 const result=grantReward(state,receipt);changed();
 if(!result.ok){toast(result.msg);showRewards();return;}
 audio.play('win');renderer.burst(analysis.owned[0]?.i??-1,REWARDS[receipt.kind].label+L(' claimed'));
 openDialog('reward-result',L`<div class="rush-result"><span>${REWARDS[receipt.kind].icon}</span><p>Test reward granted</p><h2>${REWARDS[receipt.kind].label}</h2><p>${REWARDS[receipt.kind].description}</p><button data-action="close" class="primary full">Use It in Game →</button><button data-action="rewards" class="full">View Stash</button><p class="help">In-game rewards only; they cannot be cashed out or transferred.</p></div>`);
}
function renderRichOverview(){const goal=RICH_GOALS.find(m=>analysis.wealth<m.wealth),growth=analysis.wealth-(state.startingWealth||2000000),passive=analysis.passive+analysis.investment.dividends;$('#side-content').innerHTML=L`<section class="side-section"><h3>My Portfolio Performance</h3>${row(L('Net worth change since start'),(growth>=0?'+':'')+money(growth))}${row(L('Total return'),((analysis.wealth/(state.startingWealth||2000000)-1)*100).toFixed(2)+'%')}${row(L('Rental net income + dividends'),money(passive)+L(' / mo'))}<p class="help">Net worth change includes investment P&L along with living and experience spending.</p><button class="portfolio-link" data-action="stocks">Check Stock Weighting and Dividends →</button><button class="portfolio-link" data-action="portfolio">Run My ${analysis.owned.length} Rental Properties →</button></section>${chapterCard(state,analysis)}${rivalCard(state,analysis.wealth)}<section class="side-section"><h3>Next Wealth Goal</h3><div class="milestone-title"><b>${goal?.name||L('Legacy Maker achieved')}</b><span>${goal?money(goal.wealth):'✦'}</span></div><div class="meter milestone"><i style="width:${goal?Math.max(0,Math.min(100,analysis.wealth/goal.wealth*100)):100}%"></i></div><p class="help">${goal?L`${money(Math.max(0,goal.wealth-analysis.wealth))} to go`:L('Keep growing your empire and your lifestyle.')}</p></section><section class="side-section"><h3>How I Enjoyed It</h3>${row(L('My collection'),flexState(state).owned.length+' / 3')}${row(L('Special memories'),(state.lifestyle?.memories||0)+L(''))}${row(L('Travel, culture & cruise spend'),money(state.lifestyle?.spent||0))}<button class="portfolio-link" data-action="lifestyle">Pick My Next Experience →</button></section><section class="advisor"><div class="advisor-avatar">✦</div><div><b>${analysis.net>=0?L('You may not work, but your assets do.'):L('Review your spending and asset mix.')}</b><p>Projected cash flow this month ${money(analysis.net)}. Invest it or spend it on yourself: your call.</p></div></section>`;}
function owners(){openDialog('growth',empireDialog(state,analysis.wealth));}
import {assetSaleReport} from './engine.js';
import {saleDialog,demolitionDialog} from './asset-sale.js';

$('#app').addEventListener('input',e=>{if(e.target.id==='index-units')indexEstimate();});
$('#app').addEventListener('input',e=>{if(!['startup-industry','startup-concept','startup-amount'].includes(e.target.id))return;const industry=$('#startup-industry').value,concept=$('#startup-concept').value,q=startupQuote(industry,concept);$('#startup-amount').min=q.min;$('#startup-estimate').textContent=startupEstimate(industry,concept,$('#startup-amount').value);});
$('#app').addEventListener('change',e=>{if(!e.target.dataset.startupStrategy)return;const r=setStartupStrategy(state,Number(e.target.dataset.startupStrategy),e.target.value);if(r.ok){changed();openDialog('growth',startupsDialog(state));}toast(r.msg);});

// Mobile operating systems may suspend the page without firing beforeunload.
document.addEventListener('visibilitychange',()=>{if(document.hidden)save();});
window.addEventListener('pagehide',save);
